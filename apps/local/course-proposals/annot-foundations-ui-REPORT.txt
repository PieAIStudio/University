foundations-ui English annotation report
============================================================
Lessons processed: 12 / 12
Total anchors: 51
Vocabulary: data/vocabulary/en.json (240 entries)
All senseIds verified present: YES
Hard substring traps remaining: 0 (构建/构建工具 allowed as soft)

## Spot checks (8 anchors recounted)
  PASS a-component-is-a-function/props-are-parameters '默认值'#1/2 …里指出：哪些是调用方必须给的、哪个有默认值、props（属性）和普通函数参数是…
  PASS a-component-is-a-function/props-are-parameters '接口'#1/1 …在 `PlayerAvatar` 的接口和参数列表里指出：哪些是调用方必须给…
  PASS a-component-is-a-function/jsx-is-not-html '返回'#2/5 …e`。↵↵一句话记：**组件就是一个返回界面的函数。**↵↵## 自检↵**…
  PASS composition/render-nothing-sometimes '空值合并'#1/1 … 1)` | 总要显示一个字符 | 空值合并：没给 avatarLabel 就用…
  PASS composition/rest-props '剩余参数'#1/2 …钮属性，以及 `...props`（剩余参数 / 展开）在参数列表和 JSX 里…
  PASS composition/rest-props '解构'#1/3 …uttonElement>`；参数里解构出 `children`、`clas…
  PASS state-and-effects/cleanup-is-not-optional '订阅'#2/5 …件卸载或 effect 重跑前执行。订阅与定时器若不取消，会在「人已经走了」…
  PASS state-and-effects/state-is-memory '状态'#1/4 … 里的 `useState` 说出：状态（state）存在哪、`setSta…

## Substring collision adjustments
  - compute-then-return: 用「重新渲染」整词，不用「渲染」子串
  - curly-braces: 弃用「字面量」(落在字面量字符串/对象字面量内)；弃用「返回」(落在返回值内)；改用「默认」「参数」「子节点」「展开」
  - jsx-is-not-html: 「返回」occurrence=2 避开「返回值」；「构建」在「构建工具」内保留（build工具）
  - props-are-parameters: 用「默认值」整词；弃用「解构」(落在解构参数内)，改用「接口」
  - accessibility: 弃用「值」(布尔值/属性值)，用标题「属性」
  - render-nothing-sometimes: 「空值合并」整词；「渲染」取标题第1次
  - rest-props: 「剩余参数」整词；「解构」取「解构出」(非解构参数)
  - variants: 「联合类型」「类型检查」整词；弃用「可选」(可选项)，改「组件」
  - cleanup: 「清理函数」整词；「订阅」occurrence=2 避开「事件订阅」
  - ref-is-a-pocket: 「重新渲染」整词；「口袋」→ ref.pocket
  - state-is-memory: 「状态」#1 为「状态（state）」，避开「状态文案」

## Per-lesson anchors

## a-component-is-a-function/children-is-a-prop-too
  ✓ '组件' #1/4 → component.ui (component)
    事实：组件签名是…
    …↵## 先给结论↵- **事实：组件签名是 `({ children…
  ✓ '片段' #1/1 → fragment.jsx (fragment)
    Fragment（片段）
    …`** —— Fragment（片段）：需要包一层好 `return…
  ✓ '渲染' #1/1 → render.ui (render)
    几乎不渲染「自己的」界面
    …n` 是什么、为什么函数体几乎不渲染「自己的」界面，以及 `<>…<…
  ✓ '参数' #1/2 → parameter.input (parameter)
    children 参数
    …1. **`children` 参数** —— 类型是 `React…

## a-component-is-a-function/compute-then-return
  ✓ '返回' #1/2 → return.value (return)
    在返回界面之前做计算
    …函数体 = 普通函数体；你可以在返回界面之前做计算。屏幕上看到的，是…
  ✓ '子节点' #1/1 → children.nodes (children)
    填进 style 和子节点
    …算好的值填进 `style` 和子节点。↵↵没有这些先行计算，JSX …
  ✓ '组件' #1/6 → component.ui (component)
    组件函数并不是只能写 return
    …、为什么颜色和首字要先算好，以及组件函数并不是「只能写 return…
  ✓ '重新渲染' #1/2 → render.ui (render)
    父组件重新渲染 — 整词
    … 自检↵**问：** 每次父组件重新渲染导致 `PlayerAvatar…

## a-component-is-a-function/curly-braces-are-an-escape-hatch
  ✓ '子节点' #1/3 → children.nodes (children)
    以及子节点 {initial}
    …dth: …, … }}` 以及子节点 `{initial}`。**↵…
  ✓ '展开' #1/1 → spread.copy (spread)
    剩余 props 展开到元素上
    …ops}`：把剩余 props 展开到元素上（后面《rest-pro…
  ✓ '参数' #1/1 → parameter.input (parameter)
    参数里的变量
    …e}`：右边的 `type` 是参数里的变量。↵- `{...pro…
  ✓ '默认' #1/1 → default.value (default)
    JSX 默认像标记语言 — 认 default
    …`。**↵- **推论：JSX 默认像标记语言；一旦需要计算值、读变…

## a-component-is-a-function/jsx-is-not-html
  ✓ '组件' #1/3 → component.ui (component)
    组件的本体仍是函数
    …nput(...)`** —— 组件的本体仍是函数。前几门课练过的「…
  ✓ '返回' #2/5 → return.value (return)
    返回界面的函数 — 避开返回值
    …。↵↵一句话记：**组件就是一个返回界面的函数。**↵↵## 自检↵…
  ✓ '参数' #1/2 → parameter.input (parameter)
    参数进去、值出来
    …的本体仍是函数。前几门课练过的「参数进去、值出来」在这里变成「参数进…
  ≈ '构建' #1/1 → build.action (build)
    构建工具
    …引号包起来的 HTML 字符串。构建工具会把它编译成函数调用；你现在…

## a-component-is-a-function/props-are-parameters
  ✓ '属性' #1/4 → props.parameters (props)
    props（属性）
    …给的、哪个有默认值、props（属性）和普通函数参数是什么关系。↵↵…
  ✓ '参数' #1/10 → parameter.input (parameter)
    props 就是函数参数
    …# props 就是函数参数↵↵## 学习目标↵完成本课后，…
  ✓ '默认值' #1/2 → default.value (default)
    哪个有默认值
    …出：哪些是调用方必须给的、哪个有默认值、props（属性）和普通函数参…
  ✓ '可选' #1/2 → optional.choice (optional)
    可选的 size?
    …`seed`、`alias` 和可选的 `size?`；函数写成 `…
  ✓ '接口' #1/1 → interface.shape (interface)
    PlayerAvatar 的接口和参数列表
    …`PlayerAvatar` 的接口和参数列表里指出：哪些是调用方必…

## composition/accessibility-in-two-attributes
  ✓ '状态' #1/3 → state.memory (state)
    有的给样式状态
    …」服务——有的给读屏，有的给样式状态，阅读代码时要问「这是写给谁的」…
  ✓ '子节点' #1/1 → children.nodes (children)
    装饰性子节点该藏
    … alias 也读不到——装饰性子节点该藏，可交互的整钮通常不该整颗藏…
  ✓ '属性' #1/6 → props.parameters (props)
    两个属性，写给不同的读者
    …# 两个属性，写给不同的读者↵↵## 学习目…

## composition/render-nothing-sometimes
  ✓ '渲染' #1/8 → render.ui (render)
    有时什么都不渲染
    …# 有时什么都不渲染↵↵## 学习目标↵完成本课后，…
  ✓ '空值合并' #1/1 → nullish.coalesce (nullish)
    空值合并：没给 avatarLabel
    …)` | 总要显示一个字符 | 空值合并：没给 avatarLabel …
  ✓ '兜底' #1/1 → fallback.plan (fallback)
    做兜底
    …s.slice(0, 1)` 做兜底；元信息行写成 `{meta &…
  ✓ '三元' #1/1 → ternary.operator (ternary)
    改用三元或显式判断
    …：有料才画；碰到数字 0 要改用三元或显式判断。**↵…

## composition/rest-props
  ✓ '剩余参数' #1/2 → rest.params (rest)
    剩余参数 / 展开
    …性，以及 `...props`（剩余参数 / 展开）在参数列表和 JSX…
  ✓ '展开' #1/5 → spread.copy (spread)
    剩余参数 / 展开
    …..props`（剩余参数 / 展开）在参数列表和 JSX 里各做了…
  ✓ '解构' #1/3 → destructure.unpack (destructure)
    参数里解构出 children… — 独立「解构出」
    …tonElement>`；参数里解构出 `children`、`cl…
  ✓ '属性' #1/5 → props.parameters (props)
    原生按钮属性
    …`disabled` 等原生按钮属性，以及 `...props`（剩…

## composition/variants
  ✓ '变体' #1/3 → variant.appearance (variant)
    变体名编进 class
    …variant}` `` 如何把变体名编进 class。↵↵## 先…
  ✓ '联合类型' #1/3 → union.type (union)
    ButtonVariant 联合类型
    …`ButtonVariant` 联合类型、默认 `variant = '…
  ✓ '默认' #1/2 → default.value (default)
    默认 variant = secondary
    …onVariant` 联合类型、默认 `variant = 'sec…
  ✓ '类型检查' #1/1 → typecheck.static (typecheck)
    类型检查时报错
    …合法，传 `'huge'` 会在类型检查时报错。默认 `'seconda…
  ✓ '组件' #1/1 → component.ui (component)
    同一个 Button 组件
    …一个（同一个 `Button` 组件），色号在标签上切换。↵↵## …

## state-and-effects/cleanup-is-not-optional
  ✓ '清理函数' #1/5 → cleanup.effect (cleanup)
    清理函数：离开时要收尾
    …# 清理函数：离开时要收尾↵↵## 学习目标…
  ✓ '卸载' #1/5 → unmount.lifecycle (unmount)
    在组件卸载或 effect 重跑前
    …函数（cleanup）——在组件卸载或 effect 重跑前执行。订…
  ✓ '挂载' #1/2 → mount.lifecycle (mount)
    挂载时订阅一次
    …e`。依赖数组 `[]` 表示：挂载时订阅一次，卸载时清理——与「只…
  ✓ '订阅' #2/5 → subscribe.events (subscribe)
    订阅与定时器 — #2 避开「事件订阅」
    …载或 effect 重跑前执行。订阅与定时器若不取消，会在「人已经走…
  ✓ '组件' #1/4 → component.ui (component)
    在组件卸载
    …清理函数（cleanup）——在组件卸载或 effect 重跑前执行…

## state-and-effects/ref-is-a-pocket
  ✓ '口袋' #1/4 → ref.pocket (ref)
    不触发重渲的口袋
    …useRef：改了也不触发重渲的口袋↵↵## 学习目标↵完成本课后，…
  ✓ '重新渲染' #1/4 → render.ui (render)
    不需要重新渲染界面
    … 需要跨渲染记住，但改它并不需要重新渲染界面——所以放进 ref 这只「…
  ✓ '更新' #1/1 → update.action (update)
    不驱动界面更新
    …# 重点↵**要记住但不驱动界面更新的值，放 useRef 的 `.…
  ✓ '返回' #1/1 → return.value (return)
    setTimeout 返回的句柄
    … | `setTimeout` 返回的句柄，供 `clearTime…

## state-and-effects/state-is-memory
  ✓ '状态' #1/4 → state.memory (state)
    状态（state）存在哪
    …的 `useState` 说出：状态（state）存在哪、`setS…
  ✓ '重新渲染' #1/5 → render.ui (render)
    重新渲染（re-render）
    …tStatus` 做什么，以及「重新渲染（re-render）」在这里的…
  ✓ '组件' #1/5 → component.ui (component)
    组件自己的记忆
    …# useState：组件自己的记忆↵↵## 学习目标↵完…
  ✓ '更新' #1/2 → update.action (update)
    更新 state 会让 React…
    …件函数多次调用之间仍保留的记忆；更新 state 会让 React …
  ✓ '类型' #1/1 → type.kind (type)
    记忆的类型是「字符串或 null」
    …ll>(null)` | 记忆的类型是「字符串或 null」，初始值…

## Coverage checklist
  [x] a-component-is-a-function/children-is-a-prop-too — 4 anchors
  [x] a-component-is-a-function/compute-then-return — 4 anchors
  [x] a-component-is-a-function/curly-braces-are-an-escape-hatch — 4 anchors
  [x] a-component-is-a-function/jsx-is-not-html — 4 anchors
  [x] a-component-is-a-function/props-are-parameters — 5 anchors
  [x] composition/accessibility-in-two-attributes — 3 anchors
  [x] composition/render-nothing-sometimes — 4 anchors
  [x] composition/rest-props — 4 anchors
  [x] composition/variants — 5 anchors
  [x] state-and-effects/cleanup-is-not-optional — 5 anchors
  [x] state-and-effects/ref-is-a-pocket — 4 anchors
  [x] state-and-effects/state-is-memory — 5 anchors
