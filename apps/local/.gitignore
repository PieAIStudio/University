node_modules/
dist/
.university-local-build/
coverage/
*.log
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example
.scratch/
university-local.config.local.json

# PGS skill symlinks are intentionally visible in Git as part of the internal
# checkout setup contract. Their relative targets require the sibling
# ProjectGovernanceSystem checkout; .pro-gov/assets.lock.json records the exact
# assets and the control-plane plan can recreate them after checkout.
.pro-gov/agent-assets/

# Personal campus data stays visible at ./studies but must never enter source control.
# The leading slash is load-bearing: an unanchored `studies/` matches a directory
# of that name at any depth, and this repository has a source module at
# server/studies/. Without the anchor Git silently swallowed it, and a fresh
# clone could not build. Keep only the root ignore contract and explanation so
# fresh clones satisfy router/link checks without tracking learner contents.
/studies/*
!/studies/.gitignore
!/studies/README.md

# Session handoff notes — working material, not project documentation.
.handoff/

# Quality-loop working material: screenshots, evidence dumps, and handoff notes
# produced while investigating a session's findings. The path says `tmp` because
# it is — the durable outcome is a learning under docs/reference/learnings/.
/artifacts/**/tmp/
