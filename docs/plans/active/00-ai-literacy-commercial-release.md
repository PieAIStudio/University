---
id: PLAN-AI-LITERACY-COMMERCIAL-RELEASE
title: "AI 基础星球：真实来源双系列与商业体验交付"
type: plan
status: active
canonical: true
owner: ai-assisted
created: 2026-09-14
last_reviewed: 2026-09-16
domain: product
tags:
  - ai-literacy
  - course-authoring
  - reality-sources
  - account-recovery
related:
  - REF-CURRENT-WORK
  - PLAN-PRODUCT-COMPLETENESS
  - PLAN-AI-FOUNDATIONS-REVIVAL
  - REF-PAYMENT-BACKEND-GAP
  - SPEC-0001
---

# 真实来源双系列与商业体验交付

## 最新结论：课程已在官网，账号服务仍有独立前置条件

两条真实来源双语课程已通过正式门禁并部署到
[University 官网](https://university.pieaistudio.com/planet?lang=zh-CN)。
发布源代码为 `dd04f8ac991911759242dd329d66cd718f49792d`；课程主实现提交为
`947a9f16`，随后提交只修正两项浏览器采集前提。此处不再把已完成的课程、
英文界面、提交推送和官网发布当成待办。后续只改确实复现的新问题。

本计划仍 active，因为真实邮箱收件、真实跨设备账号与经营验收不能由自动测试替代。
品牌 AuthKit 正常私包安装现已完成，注册/验证码/恢复/修改密码的消费端也已接入；
前轮的 401 不再是现行阻塞。账号删除采用真实受理申请、人工核查共享身份范围的
方案，不自动级联抹除其他产品。真实收费仍按 Owner 指令关闭。
历史逐轮取舍、原始失败和未登录/无效参数等记录已保存在
[执行历史](../../archive/execution/ai-literacy-release-through-dd04f8ac.md)，不默认重读。

## 授权与终点

Owner 授权本轮直接实现、循环评审、改写不合格课程、导入正式课程、验证、
commit/push 并确认官网更新；不再等待中途逐课审批。真实收款开通仍等待 Owner，
不得创建商户、购买服务、虚构经营资料或开启真实扣款。只操作现有主目录与 main；
保留已有成果、旧修订、学习记录与失败证据。删除分支或工作树前必须证明成果已合入、
无未提交资料且无其他执行者使用；禁止 reset、clean、强制 checkout、强推、绕过门禁。

交付是正式课程和实际产品，不是另一个等待审核的研究区。两条系列置于 AI 基础星球：

- 《认识 AI，从这里开始》/ Understanding AI: From Fear to Confidence。
- 《用 AI，把日常事情做好》/ AI for Real Life。

首版修订为认识 AI 6 单元、36 节，日常实践 5 单元、30 节，共 66 节；每系列不得少于 5 单元、30 节。
2026-09-15 课程整读发现原计划第二系列的第六单元重复前五单元已有的定目标、选材料、
改请求与验收流程，决定不把同一方法再扩成六篇课文。独立作品与交接验收并入第五单元
末课；原先未发布的提纲保留在 curriculum.json 的 retiredBeforePublication，不删除已发布
课程或改动学员进度。这是减少重复，不是降低每门至少五单元三十节的要求。
允许依据资料与试读增删重排，但不为数量灌水。主线面向没有编程经验的成人；
第一节就产生实际收获，不以发展史、开发环境或专业词汇为入门门槛。
中文供 Owner 阅读，英语是首发完整学习体验。已有应用/网站/游戏路线保留。

## 工作清单与验收

| 阶段 | 工作 | 可接受的完成证据 | 状态 |
| --- | --- | --- | --- |
| 0 | 核对当前文件、工作进程、课程管线、共享能力、模型登录与部署路径；写入本计划 | 当前 Git/模型预检与入口事实，明确已做和未做，不依赖旧汇报 | 已完成；当前仅操作 main；Owner 后续新增的互动实验工作树保留不动；Vercel Git 自动部署关闭，必须另外发布 |
| 1 | 在既有 evidence 合同内支持新闻、论文、公开材料、产品案例与可复现观察；补足来源元信息、可点击呈现、校验及教学约束 | 新旧课程兼容；坏 URL/缺支持信息/伪造仓库锚点的反例被拒；来源支持具体主张，而非只有 HTTP 200 | 已实现并有单测及真实浏览器证据；沿用 URL evidence，不伪造仓库 |
| 2 | 完成两系列逐课地图和经阅读核验的真实素材；先制作不同形状的样板课，评审后展开单元 | 每节目标、来源所支持事实、知识前置、互动动作、独立练习已对应；无无来源的虚构新闻或实测 | 已完成；材料与原始取舍在 ai-literacy 的 sources/curriculum/editorial-review |
| 3 | 通过已有课程技能与 CLI 生产、检测、修订、润色并发布两系列 | 66 节修订目标；每单元回看一次，每系列整读一次；至少一次独立模型检测；真实修订与拒绝稿留痕 | 已发布：36+30 节，11 单元，中英文正文/来源/互动/独立练习；见下方精确发布回执 |
| 4 | 注册/验证/登录/找回与修改密码、登出、账号删除入口和安全边界；英文错误与回到原课 | 相关共享能力复用、聚焦红绿测试、真实浏览器证据；无服务端删除能力时不伪造成功 | AuthKit 0.1.9 实际接入，共享账号表单/恢复/修改与安全继续学习已实现，14 项账号专项浏览器通过；删除为申请待核查，不冒充已删除；真实收件待 Owner 实测 |
| 5 | 小白入口、个人页下一步、提示与手动难度、保存和跨设备边界、公开商品与支付前状态 | 从 AI 星球到课文、来源、互动、练习、保存、恢复的完整路径；支付未开通不会扣款或授予权益 | 本地浏览器学习与保存恢复已通过；真实账号跨设备、邮件、收费仍未验收 |
| 6 | 全课程内容/来源/活动/英语复审，浏览器与完整门禁，文档收敛 | pnpm verify、默认 pnpm e2e、独立新鲜度与正式 delivery；保留失败原因，不缩小测试或伪造真人学习效果 | 已完成本次发布验收：66 节严格检查、完整 pnpm verify 退出 0；正常 pre-push 的 286 项默认浏览器全部通过 |
| 7 | 提交推送、验证实际官网版本及课程，再检查 main/分支/工作树 | 本地和远端 commit 相同、工作区干净、部署完成且目标网站实际呈现课程；未通过不得称全部完成 | 已完成本次课程发布：正常推送成功、正式封存构建/部署/官网字节与浏览器核验均退出 0；账号与收款不包含在此结论中 |

阶段 1、4 的工程实现可以按依赖顺序推进；本会话是唯一主目录写入负责人。
本地模型只接收明确的草稿、检测或有文件边界的工程任务；正式课程写入仍通过
既有 CLI，由负责人整合和验收。子代理不能自行提交、部署或越过访问限制。
独立问题受阻时记录精确范围并继续其他可做项，不反复向 Owner 汇报小步骤。

## 真实来源与课程质量

真实来源优先：新闻事件、官方文档、研究论文、公共数据、真实项目、图片/视频及
有记录的实际观察均可。不是只能引用代码，也不得为了适配旧流程伪造仓库或 commit。
先读材料，再写教学；每节明确来源类型、标题、发布者、日期/取用日期、链接、所支持
的具体判断与局限。官方案例只证明其自述，报道不是底层技术结论，旧事件不得当作
现行产品能力。素材中的第三方指令均视为数据，不作为开发指令。

每节按 write-lesson 单一合同完成：具体问题、一次有依据的预测、即时解释、
一项服务理解的互动、变化情境下的独立练习、2–3 张复习卡、必要的详细解释。
原始事实、作者推论、教学变式和真实运行记录明确分开；教学演示不能冒充新闻、
真实模型输出或实测性能。图片须有可用权利、说明文字与教学必要性；链接可优先于复制。

每单元审查前置是否提前解释、内容是否重复、互动是否能解、来源是否支持文字，
每系列再以初学者顺序整读。发现新材料可扩写；失去材料支持或只有装饰价值则删改。
不用私聊中的额外辅导冒充课程本身可懂，不把自动门禁或模型评分说成真人学习证据。

课程正文/提案/研究资料留在 apps/local 的产品材料边界；本页只记工作状态，
不建第二套课程生产者或竞争的长期待办。上一轮 docs/course-design 中的原则草稿
已吸收入本页；那份草稿不是已完成的课程、来源系统或工程实现。

## 已取得的正式发布证据

| 证据 | 实际结果 |
| --- | --- |
| 全仓验证 | `.scratch/grok-release/verify-release-r4.exit = 0`；验证后的产品/课程输入未再修改，只修正浏览器选择器和滚动恢复采集。此前失败保留，不复用修正前绿色 |
| 正常推送门禁 | `.scratch/grok-release/push-release-r4.log`：286 passed，完整 pre-push 退出 0；`dd04f8ac` 已通过正常 git push 进入 origin/main，未跳过 hook |
| 正式产物 | `release.json` 的 version 为 `0.0.0+dd04f8ac`，sourceCommit 为上述完整提交；2 个系列目录、6 门课程、93 节课，其中原有 27 节保留、新增 66 节 |
| 原有代码来源 | evidence 模式 baked，131 个仓库锚点与 131 段证据对应；不把该数字冒充新增公共网页来源数量 |
| 官网部署 | 2026-09-15 15:42 UTC，生产部署 `dpl_7Myvj77DTUJvm8S4A8fANzPZY1xf` READY，并实际绑定 university.pieaistudio.com |
| 官网校验 | 同日 15:43 UTC，48 个课程/来源/图片/入口文件的长度与 SHA-256 匹配，8 个中英文首末课页面通过，AI 基础星球实际进入新课程；0 failure |
| 账号按钮 | 官网实际可见宽度与原生按钮同为 276px，右侧真实命中原生按钮；无账号提交，不冒充邮件或 AuthKit 接入 |
| Git 拓扑 | 发布后 main 与 origin/main 同为 dd04f8ac，工作区干净；只存在 main 和一个主工作目录，无待删开发分支或工作树 |

以上生产核验使用真实浏览器和官网文件。手机是 390px 视口，不是实体手机；
没有创建账号、发送邮件、代学员提交练习或开启扣款。完整本机回执位于
`.scratch/final-release-20260915/pipeline-state.json`、`production/receipt.json`，
与对应构建、发布、核验日志及实际截图。该忽略目录不假装随 Git 传送；
本表、源素材、课程恢复包与课程审查摘要是可移交的正式记录。

### Owner 可直接阅读

- [认识 AI，从这里开始](https://university.pieaistudio.com/ai-literacy/understanding-ai?lang=zh-CN)：6 单元，36 节。
- [用 AI，把日常事情做好](https://university.pieaistudio.com/ai-literacy/ai-for-real-life?lang=zh-CN)：5 单元，30 节。
- URL 的 `lang=en` 使用英语界面与正文；`lang=zh-CN` 用于中文阅读。

## 仍待处理的独立账号与经营边界

### 当前账号接续（2026-09-16）

两门新课程与此前官网版本保持原样；本轮只收尾共享账号与必要产品转接。
私有包通过正常受权安装路径已实际安装精确版本
`@pieaistudio/swimmer-auth-kit@0.1.9`，旧 401 不再是现行阻塞。
凭据未进入源码、模型、前端或 CI；文档 CI 使用隔离的公共治理工具，外部分支
不取得私包读取权限。正式部署仍是已有受控本机构建产物。

| 范围 | 当前结果与验收边界 |
| --- | --- |
| Kit 消费 | 复用同一个 SDK/client/storage 与 UUID；AuthForm、UpdatePasswordForm 和 createAccountSession 已接入。产品仅保留学习回跳、访客合并、隐私与业务转接。 |
| 注册/登录 | 邮箱密码、确认重发、验证码及错误恢复；保持等待中的实例、邮箱和单次请求，结束后清空密码/验证码，不让迟到结果复活旧身份。 |
| 恢复/修改密码 | 固定 callback/reset 路由，SDK 验证当前注册身份；仅有查询参数不授权密码更新。退出/换号清理旧验证状态。个人主页修改密码按需展开。 |
| 继续学习 | 只保存合法内部课程标识，不保存认证参数或答案；入口必须仍在当前目录。跨账号不复用结算画面，回到该账号实际课节。 |
| 删除申请 | `university.request_account_deletion` 真实持久化待审请求；近期 password/OTP AMR、活跃会话、明确确认与预期身份匹配。请求绝不等于已删账号，也不清理钱包或其他产品。 |
| 邮件/跨设备 | SMTP 与 code-only 模板经真实只读检查确认。真实 Data API 双账号/隔离在 staging 验证；真实生产收件箱及实体设备仍未测试，不用模拟服务冒充。 |
| 收费 | 按 Owner 指令继续关闭，未改公开课程、价格、权益或商户配置。 |

Grok CLI 工程任务实际使用 `grok 1.0.30` / `grok-4.6` / `xhigh`，
两个有界子任务的日志和退出码位于 `.scratch/account-closeout/` 及 Backend
受管工作树的 `tmp/university-auth-closeout/`。上轮达到 turn limit 的部分代码
完整保留后修正，不恢复 HEAD 制造红灯，不以代理报告代替负责人验收。

独立复核没有接受“排除整个 Backend 测试目录来解决类型错误”的做法：已恢复测试
参与 typecheck，只给实际读取 Node 文件的测试明确 Node 类型；全仓 typecheck 已通过。
账号说明改为按需展开后旧浏览器 locator 命中两个 summary，按真实账号入口定位修复，
保留失败与新的精确检查。局部验收脚本现每次保留独立日志/截图目录，避免覆盖证据。

### 账号删除候选的淘汰与真实受理边界

Grok 初版尝试用外键扫描判断可安全删除共享账号。负责人复核发现间接产品关联与
非外键 Storage 所有权不能据此证明安全，且缺少页面确认身份与请求身份的相等条件。
该自动擦除候选在任何云端应用之前淘汰，原件/测试保存在
`tmp/university-auth-closeout/deletion/rejected-automatic-delete/`；本机自动清理
候选也退出产品，没有把退出或清缓存算成删除。

最终迁移 `20260916020000` 只提供真实待审队列。前端写明“申请删除账号”，需
同账号重新验证并输入 `REQUEST ACCOUNT DELETION`，返回真实请求编号。重复请求
沿用原编号，网络响应不明仍可幂等重试；15 秒中止等待不冒充服务器没收到请求。
删除本身需要负责人员核查跨产品资料、资产、订阅与保留义务后处理，本轮没有提供
自动全品牌擦除器，也没有承诺处理时限。运营职责及禁用流程见 Backend runbook。

### 当前收据

- Backend 回跳/模板只读工具与另一维护线合并，78 项 Auth/hardening 回归通过，
  原 code-only 模板、SMTP 和验证策略不变；生产 Auth Readiness `35042758503`
  在 `d967b94` 成功，不等于邮箱实际收到邮件。
- Backend 请求迁移提交 `26f121bdd3545b8bc14cca2a2ada68f6e278d0c2` 已推送。
  实际迁移 PGlite、权限/回滚 SQL 及旧 University 合约合计 25 项通过；硬化 58 项、
  静态迁移、graduation、文档与 JavaScript 语法检查通过。
- 同一提交的 Staging Preview `35045722217` 全程成功，恢复及最终暂停固定的
  data-less staging。真实临时 A/B 账号完成请求与重试、预期身份拒绝、RLS 隔离、
  原学习文档保留；未删除任何既有用户。这是实际 SDK/Data API，而不是 UI 的模拟响应。
- University 共享账号/回跳/申请删除 UI 局部 26 项通过；请求传输与真实 Kit 装配
  12 项通过。全仓类型检查通过。最新 14 项浏览器专项含真实 App 的 Kit 登录失败→
  重试→回到实际发布课程，以及中英文请求/验证码/密码恢复；全部通过。
  这些浏览器仅在 SDK 网络边界使用合成服务，不发送真实邮件、不创建生产用户。
- 看图复核后补上英文头像账号入口的可访问名称。此一修改之后最终完整 verify、
  正常 pre-push 浏览器、封存构建与官网检查另跑，不复用修改前的绿色。

本次准备生产临时账号浏览器脚本的写入被工具安全检查拒绝，文件未生成、未执行；
没有换代理/凭据路径重放。真实收件箱、实体设备和最终人工删号仍是明确的外部验收，
不会用工作区干净或网站发布替代。支付仍等待 Owner 开通。

最终发布收据在获得成功退出码和官网实际版本后记录。不要重复生产 66 节课，
不要把相邻 Backend 主目录中其他 Owner 的改动清掉；仅整理本任务实际拥有的分支。

### Owner 主线走查收口（2026-09-16）

现有 `pnpm start` 已在主目录启动交付 9998、作者 9999、API 4317。两模式各自
的两门首课、来源、互动及共享 Kit 找回密码表单，六项实际浏览器检查通过；
没有提交账号、邮件、判题或付款请求。首次探针未展开账号折叠区而超时，修正
采集动作后重验，不冒充产品缺陷。入口、重启和截图方法在
[本机走查说明](../../reference/execution/local-device-testing.md)。Owner 指定的互动
实验工作树保留，不合入、不删除、不停止其服务。

Backend 同 SHA production `35046614727`、`35046763752` 成功，包括请求权限
检查及生产迁移一致性；不以重复发布增加验收计数。实际能力仍是受理待核查的
请求，非自动擦除账号。另发现本机两端缺少精确回跳路径，新增八个 loopback
callback/reset 地址，两个反例先红后绿、72 项 Auth/email/hardening 通过，
代码 `479be65` 已推送；真实 Auth Readiness 配置与读回另记，未改 SMTP 或邮件模板。

该补充已由 Auth Readiness `35047620319` 在 `479be65` 成功应用并读回八个
精确回跳地址；SMTP 已配置、登录邮件为验证码单入口、原验证策略均保持不变。
邮件模板检查为只读且 PATCH 次数为 0。回跳配置成功仍不证明邮件实际收件。

Grok 有界只读复核退出 0；逐项负责人结论在本机 `owner-supervisor-review.md`。
没有采用将瞬时 PASSWORD_RECOVERY 事件作为授权的建议；真正的密码更新仍由
共享 Kit 验证服务端用户，再交给官方 SDK/提供商的安全策略。无账号验证的 URL
标志不授予写权限，真实邮箱收件与生产密码操作仍不据此冒充已验收。

本轮完整 verify 首次只在未改动的地形矩阵超时（80.7 秒/原 60 秒），其余 989
项 world 通过；同一矩阵独立重跑 22.4 秒通过。原失败日志保留，未增加超时、
删减断言或修改渲染器。后续完整验证结果按实际退出码记录。

随后相同产品代码的 `VITEST_MAX_WORKERS=2 pnpm verify` 完整退出 0
（`owner-final-verify-r2.log/.exit`），包含全部模块、构建、来源新鲜度、互动与文档。
该矩阵本轮 20.1 秒通过，原 60 秒不变。下面通过正常提交/推送门禁完成本次
账号版本交付，保留 Owner 已经能打开的主线预览；不再追加新功能或改写课程。

### 共享表单切换后的验收修正

账号实现提交为 `e816f714`。接续中只修旧浏览器探针，不再改动已验收产品代码：
折叠入口限定直接子 summary，账号字段按原生 email/password 类型定位；缺密码
验证检查浏览器真实 required 状态与焦点，而不是要求已退役表单的自定义文案。
空表单按钮按品牌 Kit 合同禁用，响应测量先填写合成凭据，并在网络边界明确返回
一次合成的错误密码响应；真实服务不会收到这份登录请求。测量仍保留 300ms 上限、
实际指针命中、错误提示、邮箱保留、密码清空和单次请求断言。

首次复跑的两个失败来自刻意返回的 HTTP400 被通用 console 守卫当成意外错误。
只对本测试真实拦截的精确 URL 与状态码接受浏览器资源错误；JavaScript 异常及
其他 URL 的 400 继续失败。新增真实浏览器反例已证明三条边界，不关闭 console
检查或修改产品。修正后五项专项全部通过，格式、lint 与 diff 检查通过；证据在
`.scratch/account-closeout/final-walkthrough-probes-r2.log`，初次失败原件保留。
完整默认浏览器由正常 pre-push 重新运行；最后的官网账号版本核验另记。

随后有界 Grok 静态审查确实指出探针的两项问题：合成口令与网络拦截作用域分离，
以及整个跨屏过程共享 token 400 拦截。负责人接受并修正；口令与拦截现只在个人
账号那一步同时存在，严格核对方法、grant_type、合成输入与一次请求，结束先清空
字段再撤回拦截，其他 token 请求不被默默改写。console 的期望错误只消费一次，
同 URL 第二次 400、其他 URL 400 和脚本错误均由真实反例证明仍会报错。
第一轮探索审查达到 turn limit，不算完成；后续直接输入差分的 Grok 审查退出 0，
其发现与本次修正在 `final-walkthrough-diff-review.log` 保留。

为纳入这些检查修正，已主动中断先前正常推送及其验收子进程，未部署该中间版本。
子进程结束前并发复测曾使未改课文的响应测得 417ms；结束后的相同五项专项
重新全部通过（`final-walkthrough-probes-r4.log`），300ms 门槛未变。中断的门禁
为 57 passed / 1 interrupted / 240 not run，不当成全套验收。接下来从新提交重新
正常推送并发布；不追加产品功能、不改动互动实验工作树。
