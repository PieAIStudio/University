---
id: REF-ISLAND-LOOK-CONTRACT
title: Island Look Contract
type: reference
status: active
canonical: true
owner: human
created: 2026-08-28
last_reviewed: 2026-09-01
domain: web3d
tags:
  - island
  - world-map
  - technical-art
  - visual-validation
---

# 岛屿观感合同：可以量的漂亮

## 名字先对齐（三层，各有确切的代码对应）

| 叫法 | 代码 | 看到什么 | 在这一层选什么 |
| --- | --- | --- | --- |
| **星球页** | `packages/world/src/planet/PlanetPage` | 一个星球 + 一列「学习」 | 选哪个学习 |
| **岛群图** | `Maps.tsx` 的 `WorldScene`，`detail="world"` | 一片天空中好几座浮岛，每座挂一个课程标题 | 选哪门课（一座岛 = 一门课） |
| **课程岛** | `Maps.tsx` 的 `CourseScene`，`detail="course"` | 一座岛的近景，一串圆盘排成蛇形 | 选哪一节（一个圆盘 = 一小节） |

代码里管岛群图叫 `WorldScene` / "world map"，是历史叫法。**文档里一律用上表这三个词。**

## 这份文件解决什么

`docs/plans/completed/island-v2-vertical-slice.md`（已退役，见其顶部横幅）定的是**语义**：一条主线、双投影、unit 不靠地形切开、LOD 合同。那些都对，也都不管画面好不好看。

第一版切片跑完之后画面仍然不好看，原因不是算法失败，是**没有人写下「好看」是什么**，所以执行方只能优化它能读到的东西。六十张迭代截图里，`course-grass-*`、`course-dirt-path-*`、`course-mossy`、`course-desert`、`course-natural-soil-v3/v4`、`course-topmesh-*` 全部是地表材质的变体，**没有一张是镜头、光比、阴影或天空的变体**——而参考图之所以好看，主要就靠后面这四个。

本文件补上那四个，并且**每一条都必须能从一张 PNG 或一个单测里量出来**。量不出来的形容词不写进来。

---

## 一、诊断（带行号，不是感受）

### 1. 光比是 1.3:1，应该是 3:1 以上

`packages/world/src/Maps.tsx`：

- `directionalLight intensity={2.1}`（key）
- `hemisphereLight ... 1.35` + `ambientLight ... 0.22`（fill）

key : fill ≈ **1.3 : 1**。这个比例下阴影几乎不存在，所以所有物件看起来像贴纸，地形看起来像一块布。参考图的立体感来自 **3:1 到 5:1**。

> **2026-08-28 修订：这一条要写成区间，不是下限。**
>
> 这句「应该 3:1 以上」被照做了，然后没有人给上限。key 从 2.1 一路加到 **9.0**，
> 补光留在 0.5，比例变成 **18:1**。结果是走到了另一个极端：实测课程画面
> 1,296,000 像素里有 **309,460 个（23.9%）** 亮度低于 0.08，背光坡面整片压成纯黑，
> 地形造型、路线和课程节点全被吃掉。参考图里**没有纯黑**。
>
> 新的合同是一个**区间**：直接 key : local-fill 保持 3:1 以上，
> **含环境光的总光比落在 2:1 到 4:1 之间**，暗像素（亮度 < 0.08）占比不超过 **15%**。
> 「加光比」和「别把暗部压死」是同一条规则的两端，任何一端单独执行都会翻车。

补充：`shadow-camera` 只覆盖 `±0.3 × extent`，也就是说岛群图上只有中间 60% 的地方有影子。

### 2. 主体和背景一样暗

岛群图背景是 `PALETTE.seaDeep` 的一块圆盘 + 云。岛是中绿。两者明度接近，所以再多细节都糊在一起。参考图的岛之所以「跳」出来，是因为它坐在**接近白的云海和亮蓝天**前面——这一条和建模无关。

### 3. 密度差一个数量级

`packages/world/src/island/island-blueprint.ts`：`slotCount = Math.max(18, lessons * 3)`。
`packages/world/src/Maps.tsx` 再按 `hash > 0.58` 丢掉 42%，再丢掉离节点 3.15 以内的。
18 节课的岛最后大约 **20 个物件**。参考图上能数出来的在 **150–250** 之间。

### 4. 最该密的一圈被代码明确排除

同一文件：`radial = Math.sqrt(random()) * 0.78 * edgeScale`。物件永远放不到半径 78% 以外。
参考图里岛的「好看」有很大一部分来自**边缘**：树冠探出悬崖、灌木压在岩壁上、剪影是毛的不是光的。现行规则保证那一圈是秃的。

### 5. 镜头没有取景

课程岛的截图里根本看不到岛的边。参考图是一个 **diorama**：整座岛在画面里，四周留白，一眼能看出这是一座浮岛。

---

## 一点二五、岛群图不是拿来对标参考图的那一层

我算了一遍现有数字，结论跟直觉相反，**值得先写下来免得派错活**：

- 课程之间的间距 `STUDY_PATH.step = 7.4`；
- 岛半径 `radiusForLessons(n) = 0.55 + sqrt(n) * 0.42` —— 18 节课的岛直径 4.7，41 节课的 6.5。

也就是说**沿着那条路，陆地已经占了六成到九成的长度**，岛几乎是挨着的。排版没有问题，岛也不小。

真正的原因是：**那是一条一维的链，却被塞进一个二维的方框里取景。**
`Maps.tsx` 用 `extent = max(hypot(x, z)) + 8` 求一个外接半径去框它。一条斜着走的链
外接出来的方框，一多半是空海——3.2% 就是这么来的。

由此有三条推论，都写进任务里：

1. **参考图和 donor 白天场景都是一座岛的近景，对标的是课程岛，不是岛群图。**
   拿岛群图去比，是拿「整片海」比「一座岛特写」，比出来的差距不是缺陷。
2. **A1（陆地占比 ≥ 55%）只对课程岛的设计机位生效。** 岛群图本来就该一眼看到很多门课，
   硬把镜头推近去凑这个数，等于用信息量换画面——那是把好事做坏。
3. **岛群图真正要修的是 A4（背景层次），不是陆地占比。** 参考图里岛之外的部分不是「空」，
   是**有层次的云海加远景岛影**。我们的背景明度跨度只有 15–17，那是一块平涂，
   所以那片海读起来才像空白画布。**把背景做得值得看，比把前景放大更对。**

## 一点五、标杆是 Elemental-Serenity 的实拍，不是那三张 AI 生成图

三张 AI 生成图只说明「大概想要什么感觉」，它们不是 3D 渲染出来的，拿它们当验收基准会永远差一口气。
真正的标杆是 donor `elemental-serenity` —— 那是**真的用 Three.js 渲染出来的**。

我把它 README 的四季合成图解码测了一遍（安全内区取样）：

| 面板 | 陆地占比 | 陆地 L\* 中位 / p95 | 背景明度跨度 | 全画面 p2 / p98 | 标准差 | 草地明度跨度 / p95 |
| --- | --- | --- | --- | --- | --- | --- |
| **秋（白天，我们的基准）** | 84.3% | 64.7 / 89.5 | 74.5 | **17.9 / 99.9** | **21.1** | 82.1 / 99.8 |
| 冬（高调雪景） | 19.9% | 77.1 / 99.7 | 41.4 | 33.3 / 99.9 | 15.6 | 41.6 / 99.8 |
| 夜 | 73.9% | 24.7 / 80.2 | 95.3 | 4.7 / 100.0 | 23.5 | 30.5 / 39.4 |
| 雨（刻意压暗） | 78.9% | 14.4 / 19.7 | 14.2 | 11.5 / 33.4 | 5.7 | 4.5 / 16.5 |
| **我们线上** | **3.2%** | **36.8 / 66.2** | **15.1** | **36.2 / 63.9** | **7.0** | **31.5 / 42.5** |

**这张表最重要的一行是「秋」：p2/p98 = 17.9 / 99.9，标准差 21.1。**
和两张 AI 生成图（21.2 / 23.0）在同一档。也就是说——

> **实时 WebGL 完全做得到这些数字。门槛不是幻想，是别人已经渲染出来的现实。**

（「雨」那一栏也值得记：它标准差只有 5.7，和我们线上一样平——但那是**有意**压暗的情绪场景。
所以门槛只对**白天的设计机位**生效，刻意的暗场景不适用。这条要写进来，不然会有人拿它当挡箭牌，
也会有人误伤一个本来对的暗场景。）

### 但它的结构学不了，只有它的观感能学

donors.md 里已经写清楚了：它的地面是 **5×5 平面上的 fragment paint，不是真实高度**；水面是固定平面；
整个是**一个手工编排的场景 + 固定机位**，没有岛、没有路线、没有 LOD、不是程序化的。

所以「以它为中心做程序化地图」要翻译成一句精确的话：
**拿它的光、色和四个 shader，不拿它的场景结构。**

而 donors.md 里允许拿的那四样，恰好一对一地对上我们不及格的四项：

| 能拿的 | 修的是 |
| --- | --- |
| `Shaders/Materials/skydome/*` 天空配色与大气 | A4 背景明度跨度 15.1 → ≥ 40 |
| `Shaders/Chunks/grass/*` 草叶风变形与颜色 ramp | 草地明度 p95 42.5 → ≥ 85 |
| `Shaders/Materials/bush/*` billboard 灌木与 ramp | A1 陆地占比 / 密度——billboard 便宜，才买得起十倍的植被 |
| `Shaders/Chunks/water/*` ripple / Voronoi | 海面与云海，A4 的另一半 |

**它的媒体：owner 已决定先用（2026-08-28），同时在与作者沟通授权。** 按「隔离可用、来源待确认」
处理，规则写在 donors.md 的 `elemental-serenity` 一节。三条硬要求：

1. 每个进来的文件登记进独立清单，带 `provenance: "author-permission-pending"` 与日期，
   **和已确认 CC0 的 Kenney 分开放**，授权有结论时能精确拿掉或转正。
2. **八个 GLB 全要，贴图按需取。** 它一共只有 8 个 GLB（约 260KB）：
   `bridge` / `camp` / `tent` / `rocks` / `treeTrunks` 直接进道具库——桥和营地本来就是
   参考构图里的东西；`grass_blade` / `leaf` / `bushEmitter` 合计 4.7KB，是 shader 的几何载体。
   贴图只取 shader 和道具实际引用的那些，**不整目录搬**：目录里有多少张没人用的图，
   将来就有多少条要回答的来源问题。真正值钱的是
   `public/textures/{grass,bush,ground,water,noises}` 里的噪声与 ramp。
3. 丰富度的主力仍然是 **Kenney（CC0，逐包记了许可证与 sha256）+ billboard 植被**。
   donor 媒体是补充，不是地基——地基不能建在一个还没确认的授权上。

**待确认的关键事实不是「作者是否同意」，而是「这些素材是作者自己做的，还是他从别处下载的」。**
该仓库 README 没有任何 credits 章节。如果是下载来的，作者的许可不构成转授权。

## 一点七五、实验记录：三条死路和一个真正的杠杆

这一节是**跑出来的**，不是推出来的。方法：`pnpm dev` 起交付端，`Stage.tsx` 在 DEV 下把
R3F 的 state 挂在 `window.three`，所以可以**不改一行代码**在运行时改灯光/阴影/曝光，
截图，再用同一套指标量。每一行都是实测。

判据用「**陆地 L\* 中位 → p95 的落差**」（现状 5.2，donor 24.8，门槛 15）——
它量的是整块地有没有被照亮，撒深色草点绕不过去。

| 我改了什么 | 落差 | 结论 |
| --- | --- | --- |
| 主光强度 2.1 → 3.2 → 4.0 → 6.0 | 5.2 → 5.7 → 5.8 → **5.4** | **没用。** 整块地一起变亮，对比度纹丝不动 |
| 阴影相机 ±15.8 → ±40（覆盖全岛） | 5.2 → **5.2** | **没用。** 一点没动 |
| `gl.toneMappingExposure` 1.0 → 1.25 → 1.5 | 三张图**逐像素相同** | **这个开关是死的**，见下 |
| 太阳仰角 56° → 17°，补光归零 | 5.2 → **18.8** | **这才是杠杆** |

### 为什么强度和阴影范围都没用

**这座岛是一个圆滑的穹顶，太阳几乎在正上方**（实测光源位置 `(26.4, 47.5, 18.5)`，仰角
**55.9°**，接近正午）。正午的光打在穹顶上，镜头看到的那片顶部**朝向几乎一致**，所以
N·L 处处相同——**再强的光也只是把同一个亮度整体抬高**。摄影里正午被称作最难看的光，
就是这个道理。

阴影相机没用是因为投影本来就少：只有 63 个网格 + 173 个实例在投影，而且落在地上的
面积很小；换句话说，**在没有起伏、没有密集植被之前，阴影不是地面对比度的主要来源**。

（但阴影相机仍然是个真 bug，只是不在这一轮：`±15.8` 对上一座**约 70 单位宽**的岛，
只覆盖约 20% 的面积。等植被密起来之后它会立刻变成瓶颈。）

### `toneMappingExposure` 是死的，亮部天花板在 grade 里

三个曝光值渲染出**逐像素相同**的图。原因写在 `Stage.tsx` 的注释里：颜色管线是
`grade.ts` 的 `createGradePass()`——场景先渲进一个 linear target，再由 SwimmerRenderKit
的独立 blit 做 ACES → grade → 一次 sRGB 编码，**并且在 blit 期间把 renderer 的
`toneMapping` 强制成 `NoToneMapping`**。所以 renderer 上的曝光旋钮根本不参与成像。

**亮部的天花板在 `WORLD_GRADE`**（`grade.ts`，`contrast: 1.06`、`contrastPivot: 0.34`、
还有一个 `edgeGain: 0.76` 的 vignette 在压暗四周）。实测：**任何灯光组合都没能把
陆地 p95 推过约 80，全画 p98 推过约 85。** 要过 85 / 90 这两条线，**必须动 grade**，
而 grade 来自 SwimmerRenderKit——按品牌 kit 优先，改不动就改 kit 发版本，不在这里 fork。

### 目前找到的最好一组（仍然只过 4/6）

`太阳仰角 16° · 主光 5.0 · hemisphere 0 · ambient 0.05`：

| | 结果 | 门槛 | |
| --- | --- | --- | --- |
| 陆地中位 | 52.3 | 50–70 | ✓ |
| 陆地中位→p95 | 17.2 | ≥ 15 | ✓ |
| 全画标准差 | 18.2 | ≥ 18 | ✓ |
| 全画 p2 | 0.0 | ≤ 25 | ✓（偏低，补光归零导致暗部死黑，需要回一点） |
| 陆地 p95 | 69.6 | ≥ 85 | ✗ |
| 全画 p98 | 72.8 | ≥ 90 | ✗ |

**这组数字把第二轮的任务拆成了两半**：
一半是**把太阳放低、把补光压掉**（灯光就能拿到落差与标准差）；
另一半是**抬高亮部天花板**，那必须动 grade / kit，不是调灯光能解决的。

### 第二轮跑起来之后我又量了一组：光能修四项，修不了第五项

赛马发车后我用同一个运行时回路继续试了「低太阳 + 低补光 + 强主光」，
这是任务书让两个 agent 走的方向。结果（`measureScene` 的场景线性比值 / 像素指标）：

| 配置 | 场景比值 | 背景跨度 | 标准差 | 陆地落差 | 全画 p98 |
| --- | --- | --- | --- | --- | --- |
| 现状 | 1.77 | 14.7 | 13.4 | 11.4 | 72.3 |
| 仰角 14° · 主光 10 | **11.4** | **66.3** | **20.5** | 10.8 | 79.5 |
| 仰角 14° · 主光 20 | **20.8** | **79.7** | **24.0** | 9.5 | **90.0** |
| 仰角 20° · 主光 20 | **15.7** | **85.5** | **23.7** | 6.5 | **91.9** |

**四项一次解决**：场景比值（1.77 → 11–21）、背景跨度（14.7 → 66–85）、标准差（13.4 → 24）、
全画 p98（72 → 90+）。**背景跨度尤其值得注意——它不需要单独去做天空，
太阳一放低，海面和航拍底板自己就有了明暗层次。**

**但陆地自身的落差没动**（11.4 → 9.5），而且主光一强，陆地中位数就冲出 50–70。

**结论，并且这条要影响赛马的评分**：

> **A 段（光）能修四项，修不了「陆地落差」。**
> 那一项要的是**地形起伏**和**密集植被投下的影子**——也就是 B 段（密度）。
> 评审时不因为 `landLightnessRise` 没达标而判一个 agent 输。

还量到一件事：**补光归零会让 p2 掉到 0.0**（暗部死黑）。门槛是 `≤ 25`，
数字上过得去，但画面上是把暗部压没了。**补光要留一点**，这一条门槛拦不住，得靠看图。

### 草的固有色本身就太亮了——光还没打上去，它已经在上限

把 `island-geometry.ts` 的草地调色板换算成 L\*：

| 常量 | 十六进制 | L\* |
| --- | --- | --- |
| `GRASS_LIGHT` | `0x9cc66a` | 75.6 |
| `GRASS_WARM` | `0x87b85a` | ~70 |
| `GRASS` | `0x74a957` | **63.9** |
| `GRASS_DARK` | `0x507c4b` | 48.3 |

**基准色 `GRASS` 是 L\* 63.9，而合同要求整块地的中位数落在 50–70。**
也就是说——**在任何光打上去之前，草的固有色已经顶在允许区间的上沿了。**
光一强，它立刻冲出去（实测主光 12 时陆地中位 78.1）。

这是一个经典错误，有个名字：**把光画进了贴图里**。
调色板是照着「最终画面应该长什么样」painted 的，而不是照着「光会在它上面做什么」painted 的。

正确的做法是把固有色整体压暗一档：**基准色降到 L\* 45–50 左右**，
让阳光把它抬到 65、让阴影把它压到 35。**这样明暗才是光造出来的，不是画上去的。**
donor 的草之所以能从 18 跨到 100，靠的就是这个。

**这一条是 A 段可以做的**（材质在允许改动范围内）。

**但我把它也验了，它解不开「陆地落差」**——在运行时把所有固有色整体乘以 0.72 / 0.55：

| 固有色系数 | 陆地中位 | 陆地 p95 | **落差** | p2 | p98 |
| --- | --- | --- | --- | --- | --- |
| 1.00（现状） | 78.0 | 85.2 | **7.1** | 11.4 | 86.3 |
| 0.72 | 71.9 | 79.9 | **8.0** | 5.5 | 81.3 |
| 0.55 | 66.4 | 75.1 | **8.7** | 2.2 | 77.0 |

**中位数确实回到区间里了（78 → 66），但落差几乎没动（7.1 → 8.7），而且 p2 掉到 2.2。**
压暗固有色只是把整块地一起搬下去，仍然是一块均匀的布。

### 「陆地落差」的结论：四条路试完了，只剩一条

| 试过 | 落差 |
| --- | --- |
| 主光强度 2.1 → 6.0 | 5.2 → 5.4 |
| 阴影相机 ±15.8 → ±40 | 5.2 → 5.2 |
| 固有色 ×1.0 → ×0.55 | 7.1 → 8.7 |
| 低太阳 + **补光归零** | 5.2 → **17.2**（但 p2 = 0，暗部死黑） |
| 低太阳 + 正常补光 | 9.5 – 10.8 |

看最后两行就清楚了：**落差来自受光地面与背光地面的比值。补光一回来，背光那部分被抬起来，落差就没了。**

所以要同时拿到落差**和**有细节的暗部，只有一条路：**让更多地面真的处在投影里**——
也就是**整块地上都站着会投影的东西**，而不是只有一角有树。

> **这就是 B 段（密度）存在的理由，而且它现在有了一个可量的目标，不再是「多放点树好看」。**
> 判官量到的现状：每个课程节点 **2.39** 件装饰，门槛 7。

---

## 第二轮做完了：四条线一起才有画面（2026-08-28）

上面那句「只剩一条」写完的第二天，四条线一起落了地，合并在 `e9c01ab`。
分开看每一条都只挪动了一个数字，画面没变；四条合起来才换了一张图。

### 同一个机位、同一个种子，前后对照

| 指标 | 之前 | 现在 | 门槛 |
| --- | --- | --- | --- |
| 顶面坡度中位 | ~6° | **18°** | — |
| 陆地落差 | 11.4 | **21.6** | ≥15 |
| 陆地中位亮度 | 71.4 | **68.8** | 50–70 |
| 画面标准差 | 10.9 | **16.6** | ≥18 |
| 背景亮度跨度 | 14.3 | **45.0** | ≥40 |
| 每节点装饰件数 | 2.39 | **7.02** | ≥7 |
| 近景草地落差 | — | **48.7** | ≥15 |

### 四条线各自解决了什么

**地形没有形。** 课程岛实测 85 × 112 单位，高度规则却卡在 4.35，所以岛上最高的
山是它自己宽度的 **4%**，最陡的面约 **6 度**。六度以下，方向光在受光面和背光面之间
给不出可读的差别——这就是前面所有灯光实验都只改到曝光、改不到地面的原因。
更下面一层：网格本身也扛不住。十三圈径向环，顶点间距 1.1 单位，比两个环还窄的山
根本存不进去。现在是五十二圈。

**光没有低到能找到形。** 太阳落在 28 度，主光对补光 7.3 比。这个数字是调出来的：
低太阳合同最初落在 24 度、17.7 比，那是空地在天空下实际受光的四倍，不朝主光的
一切都掉到接近全黑，岛读起来是黄昏。

**装饰稀到读不成东西。** 4,900 平方单位上 86 件，平均 57 平方单位一件。低于某个
密度，实例被读成噪点；高过去才被读成有轮廓的一团。这不是审美分歧，是有阈值的
知觉现象。

**草是四个百分点的覆盖率。** 2,200 根平叶片，每平方单位 0.45 根。现在一个实例是
一丛 5–8 片叶，近景 16,000 丛，远景 0——在航拍机位上每根叶片都不到一个像素，它们
唯一的贡献就是让岛看起来像撒了渣子。

### 三把尺子量错了，都记在这里

这一轮真正值钱的不是上面的数字，是三次「指标过了但画面没好」，以及每次是怎么发现的。

1. **中心区对外圈的对比度**——把参考图判成不及格（见上文）。
2. **高光其实是海。** 海面眩光是加性的 2.25 叠 1.25，在镜面峰值附近加了三倍半的白，
   把画面一角烧穿。把它压下去之后，**陆地自己的 p95 从 91.4 掉到 73.5**——那些过曝
   像素一直被色相分割判成陆地。也就是说「岛上有亮部」这个读数量的是海。岛真的很暗，
   得靠调亮主光解决，不是调亮海。
3. **海被算成陆地。** 陆地色相带原本到 175，那里已经是青绿。海面按设计亮度渲染出来
   之后，五分之一的海开始计入陆地，把陆地中位抬高了八个点，而岛一点没变。树叶不会
   超过 150；150 到 185 是这个产品的水。改完之后岛占设计机位画面的 0.40 而不是 0.59，
   覆盖率下限也跟着从 0.55 重新标到 0.34——**动的是读数，不是目标。**

> 三次都是同一个习惯抓出来的：**先拿一个已知好的东西去量这把尺子，再信它对新东西的
> 判断。** 参考图、donor 实拍、以及「只改海不改岛，岛的读数不该动」这类对照实验。

### 岛群图的回归，以及它的成因

低太阳那一版把航拍底板从无光材质换成了受光的海面着色器。从课程机位看这是改进；
从岛群机位看，底板就是整个画面，**把一张手绘底图当作受光表面来打光，等于把画丢掉**。
那一层从「亮青色海加一条画出来的海岸线」退成了「一片死绿」。

两个独立故障叠在一起：底板贴图按 sRGB 上传，而材质是裸 ShaderMaterial，拿不到 three
给自家材质插的传递函数，画面暗了约一档半；海面处理又没有距离上限。现在着色器在超过
底板半径三分之二处交还给画本身。

顺带的教训：**为了指标而整体压暗天空是不行的。** 低太阳那一版把八套天空的天顶色
各压了约一档去换背景亮度跨度，算术上有效，画面上失败。跨度得从太阳辉光、云的明暗
和水里来，那些是观众看得见的光。


### 判官已知的一个测量缺陷（先记下，不在赛马中途改）

陆地是按色相分割的，而背景那张航拍底板上**画着绿色和土黄色的远景小岛**——
这些像素会被算进「陆地」。所有配置受影响一致，所以横向比较仍然成立，
但「陆地中位/p95」的绝对值偏了。

精确解是让渲染器出一张**只有岛的遮罩**（我们自己的场景可以免费渲）。
**赛马期间不动判官**——中途改尺子对两边都不公平。这条留到赛马结束后修。

### 用仓库里已有的 `measureScene()` 量了一次，结果解释了前面所有现象

`window.measureScene()` 是 `Stage.tsx` 里的 DEV 工具，返回 **grade 之前**的场景线性亮度分位数。
它是为了「grade 的常数必须是量出来的」那条基线规则写的，而这次是第一次真的拿它读数：

```
p05 0.186 · p25 0.231 · median 0.287 · p75 0.311 · p95 0.329 · max 1.346
```

**中间 90% 只跨了 0.186 到 0.329，比值 1.77 : 1——不到一档光圈。**

一个有太阳有阴影的真实场景，这个比值通常在 10:1 以上。也就是说：

> **在任何调色发生之前，场景本身就没有明暗。**

这一条把前面所有实验串起来了：

- **加强度为什么没用**：强度是乘法，把 0.186 和 0.329 一起乘大，比值仍然是 1.77。
- **放低太阳为什么有用**：它改变的是受光面与背光面的**比值**，不是乘一个系数。
- **为什么调色救不回来**：`PALETTE` 上方那段注释自己写过一句正确的话——
  「一张没有中间调的图，任何 grade 都救不回来，因为根本没有东西可以展开」。
  同一句话对亮部同样成立。

### 所以增加一条在调色之前量的门槛

| # | 标准 | 门槛 | 现在 |
| --- | --- | --- | --- |
| **S1** | `measureScene()` 的 **p95 / p05**（场景线性，grade 之前） | **≥ 4.0** | **1.77** |

**S1 是这份合同里唯一一条调色动不了的指标**，因为它在调色之前读数。
它也是最上游的一条：S1 不达标，后面所有像素指标都只能靠硬拉，拉出来也是假的。

### 更正一条我自己刚写的结论

我先前写「亮部天花板必须动 SwimmerRenderKit」。**这句话过头了。**
查了 kit 的类型定义：`GradeConfig` 确实没有 exposure，但它有 `contrast`、`contrastPivot`、
`saturation`、`vignette`——而 `WORLD_GRADE` 现在是 `contrast: 1.06`（几乎等于不做）、
`vignette.edgeGain: 0.76`（在压暗四周，正好压的是 p98）。**这些都能在 `grade.ts` 本地改，不用动 kit。**

而且 kit 对 `contrastPivot` 的文档写着：这是「最项目相关、也最常被设错的一个值」，
正确做法是量未调色的 p50 再换算——**而 `measureScene()` 给的正是那个数**。
仓库里工具和文档都齐了，只是没人跑过。

## 二、三层各自的取景合同

| 层 | 看的是什么 | 镜头 | 主体占画面 | 密度 |
| --- | --- | --- | --- | --- |
| 星球 | 有哪些学习星球 | 已有 | 已有 | 不在本轮 |
| 岛群（岛群图） | 课程之间的关系与进度 | 俯角 40–50° | 单岛 ≤ 12% | **语义 LOD：轮廓 + ≤5 个地标**，不是缩小的课程岛 |
| 岛内（课程岛） | 这门课的学习路线 | 俯角 **32–40°**，整岛入画，留白 8–12% | 60–75% | **≥ 7 件 / 每个课程节点** |

**岛群图的密度规则和课程岛不是同一条。** 52 座岛各放 150 件物件会打死手机。岛群图走既有的 `detail: "world"` 投影。这一条写在前面，因为它是最容易被执行方误解成「到处都加密」的地方。

---

## 三、能从 PNG 量出来的验收标准

判图一律用 **no-post（关掉后处理）** 的那张，避免用 bloom 把亮度刷过线。

在设计镜头下截图，取两个区域：
- **主体区** = 画面中心 40% × 40%
- **背景区** = 画面外圈 20% 宽的环

| # | 标准 | 门槛 | 参考图实测（宽 / 竖） |
| --- | --- | --- | --- |
| A1 | 陆地占画面比例（**只对课程岛设计机位生效**，岛群图不适用，理由见下） | **≥ 55%** | 秋 84.3%；AI 图 32.6% / 48.0% |
| A2 | 陆地 L\* 中位数 | **≥ 50** | 秋 64.7；AI 图 53.6 / 50.2 |
| A3 | 陆地 L\* p95——受光的那一面必须真的被照亮 | **≥ 85** | 秋 89.5；AI 图 94.6 / 91.8 |
| A4 | 背景 L\* 跨度 p5..p95——天空和云海不是一块平涂 | **≥ 40** | 秋 74.5；AI 图 63.8 / 66.9 |
| L2 | 全画面 L\* 的 p2 / p98 | **p2 ≤ 25 且 p98 ≥ 90** | 秋 17.9 / 99.9；AI 图 21.5 / 94.4 |
| L3 | 全画面 L\* 标准差 | **≥ 18** | 21.2 / 23.0 |
| C1 | 草地色相跨度（p5..p95） | **≥ 35°** | 91.6° / 50.5° |
| C2 | 草地明度跨度（p5..p95）——「有没有光影」最直接的像素证据 | **≥ 45** | 秋 82.1；AI 图 34.0 / 50.7 |
| C2b | 草地 L\* p95——阳光那一面 | **≥ 85** | 秋 99.8；AI 图 62.5 / 64.3 |
| C3 | 高饱和强调色占**陆地**面积 | **≥ 1.5%**，且 ≤ 15% | 9.5% / 1.2% |
| R1 | 课程节点被遮挡比例 | **≤ 5%** | 需守住 |
| R2 | DOM 标签底下的画面对比度 | **≥ 4.5:1** | 变亮后有回归风险，必须测 |

**陆地与背景怎么分**：用渲染器出的岛遮罩，这是精确解，我们自己的场景可以免费渲一张。
参考图没有遮罩，用色相分割近似（陆地 = 黄绿到青绿加暖色，其余为背景）。

**为什么用中位数和分位数，不用最大最小**：一个黑像素加一个白像素就能骗过最大最小。中位数骗不过。

### 这些门槛是量出来的，不是拍出来的，而且已经改过两轮

第一版是**看着参考图写**的，两条数字当场被参考图自己证伪：

- 原来的 L1 写「画面中心 40% 对外圈 20% 环」。竖版参考图在这个定义下只有 **11.7**，不及格——
  但它不难看，只是岛顶到了画面边缘，外环里有一半还是岛。**固定裁剪量的是构图，不是观感。**
- 原来的强调色写「2%–5%」。两张参考图是 **9.5%** 和 **1.2%**，把这个区间从两头包在外面。

第二版换成色相分割真的去测，又推翻了一条**我自己刚下的结论**：

- 我曾经拿「中心 vs 外环 = 3.9」说「岛和背景是同一个亮度」。**那个数不成立**——线上岛群图
  97% 是海，中心和外环取到的都是海，所以它量的是「整幅画面很均匀」（这一点 L3 = 7.0 已经说得
  更清楚），不是「岛 vs 背景」。真按遮罩分割，线上的岛与背景差是 **15.9**，而两张参考图是
  **19.8 和 13.7**——**线上这一项其实落在参考图区间里，根本不是问题所在。**
  差一点就派人去改一个没坏的东西。

**一个自己都通不过自己门槛的合同是坏合同；一个没被数据推翻过的诊断是没验过的诊断。**
先量，再定门槛，量完了要敢推翻自己。

### 现状基线（2026-08-28，**本地 main**，DOM 全部隐藏后只测渲染画面）

**两条更正，都记在这里：**

1. **一开始量的是岛群图，量错了层。** 参考图和 donor 白天场景都是一座岛的近景，对标的是课程岛。
   「陆地只占 3.2%」这条**撤回**——它描述的是一个不该拿来对标的取景。
2. **接着量的是线上，而线上落后 main 很多**（island-v2 从未部署）。基线必须量 main。

| | 陆地占比 | 陆地 L\* 中位 / p95 | 背景跨度 | 全画 p2 / p98 | 标准差 | 草跨度 / p95 |
| --- | --- | --- | --- | --- | --- | --- |
| 线上（旧岛） | 64.1% | 63.9 / 68.5 | 17.1 | 24.5 / 70.9 | 9.5 | 22.9 / 68.4 |
| **main（当前）** | **72.0%** | **66.1 / 71.3** | **14.7** | **17.7 / 72.3** | **13.4** | **48.2 / 71.2** |
| **donor 秋（基准）** | 84.3% | 64.7 / **89.5** | **74.5** | 17.9 / **99.9** | **21.1** | 82.1 / **99.8** |
| 门槛 | ≥55% | 50–70 / ≥85 | ≥40 | ≤25 / ≥90 | ≥18 | ≥45 / ≥85 |

**先给 island-v2 记一功**：标准差从 9.5 提到 13.4，草地明度跨度从 22.9 提到 48.2（已达标）。
那一轮不是白做的，它把两项推动了。

**然后是这份基线真正说清楚的一件事——`p2 = 17.7` 已经达标。**

暗部是够的。缺的**全部在亮的那一半**：

| 不及格项 | 现在 | 门槛 | 差多少 |
| --- | --- | --- | --- |
| 陆地受光面 p95 | 71.3 | ≥ 85 | 13.7 |
| 全画面 p98 | 72.3 | ≥ 90 | 17.7 |
| 背景跨度 | 14.7 | ≥ 40 | 25.3 |
| 全画面标准差 | 13.4 | ≥ 18 | 4.6 |

**所以方向不是「整体调亮」，是「把太阳和天空加进去」。** 整体调亮会把 `p2 = 17.7` 顶过 25，
反而弄坏一项已经达标的指标——这条门槛就是为了拦住这个动作而存在的。

**还有一个数字最能说明地面的状态：陆地明度中位 66.1，p95 71.3，只差 5.2。**
占画面 72% 的那块地，从头到尾几乎是同一个亮度。donor 那块地中位到 p95 差 24.8。
它是**一块平铺的绿桌布**，上面撒了一些深色草点。

**注意一个指标陷阱**：草地明度跨度 48.2 已经达标，但它是被**深色草簇**（albedo）撑起来的，
不是被**光影**撑起来的。真正不会被这样绕过去的是「陆地中位 vs p95」那一对——
它量的是整块地，撒多少深色点都改变不了它没被照亮这件事。

## 四、只能从代码里量出来的验收标准

| # | 标准 | 门槛 |
| --- | --- | --- |
| D1 | 课程岛物件数 / 课程节点数 | **≥ 7** |
| D2 | 物件分四层：canopy / mid / ground / rim | 每层至少占总数 **10%** |
| D3 | 半径 > 0.8 的物件占比 | **≥ 20%** |
| D4 | key : fill 光比 | **≥ 3 : 1** |
| D5 | `shadow-camera` 覆盖范围 | 覆盖设计镜头可见的全部地面 |
| D6 | 岛群图单岛物件数 | **≤ 8**（语义 LOD 不变） |
| P1 | 移动端帧预算 | 不劣于当前基线 |

---

## 五、判官（怎么跑）

一条命令，固定 seed、固定镜头，产出一组 PNG 加一份 metrics JSON：

1. 冻结输入：`foundations-before-zero`，seed 固定。
2. 拍 no-post 基线 + 最终图。
3. 拍近 / 设计 / 远三个机位。
4. 至少一个压力 seed（41 节课的岛）。
5. 桌面 + 390×844 各一组。
6. 输出 `metrics.json`，逐条给出上表每一项的实测值和是否达标。

这一步的意义：**把「太差了」变成「L1 = 11，门槛 25」。** 没有这一步，每一轮反馈都是一句感受，循环不收敛。

参考已注册技能：`threejs-visual-validation`（固定视角合同与拒收条件）、`threejs-camera-direction`、`threejs-exposure-color-grading`、`threejs-atmosphere-aerial-perspective`、`threejs-shadow-systems`。它们是**手册参考**，不是可安装的运行时；`threejs-awesome-graphics-agent-skills` 的降雨示例含 GPL 派生代码，不得复制进产品。

---

## 六、明确不许做的事

1. **不许出现第二份实现。** `island-blueprint` 与 `island-blueprint-v2`、`island-geometry` 与 `island-geometry-v2`、`cloud-layout` 与 `cloud-sea-v2` 必须先合成一份，再谈观感。合并前做的每一个数字都要做两遍。
2. **不许用后处理刷指标。** 判图用 no-post。
3. **不许把课程岛的密度规则套到岛群图。**
4. **不许让物件盖住课程节点**（R1）。
5. **不许因为画面变亮而牺牲 DOM 标签的可读性**（R2）。
6. **可读文字一律是 DOM，不是几何体。**
7. **`packages/ui` 的 `three` 依赖保持 0。**
8. 参考图是 AI 生成的插画。合同要的是它的**结构**——光比、明度跨度、密度、剪影、边缘——不是它的渲染精度。低模 CC0 资产做不出插画的细腻，也不需要做出来。

## 七、近景镜头复核（2026-08-28）

这一轮固定使用 `foundations-before-zero`，在桌面 1440×900 和手机 390×844
上看真实课程页面；联系表额外覆盖 `polar=56/60/64/68/72/76°`、
`distance=30/36/42`。联系表不是自动评分：先看 PNG，再用下列结论约束代码。

### 被推翻的 WIP 结论

上一轮 WIP 把 `roadWidth=0.68`、`shoulderWidth=0.16` 当成了最终宽路，理由是
它在单张候选图里更醒目。复核发现这会让蓝图把 hero 推到更外侧，
`island-dressing.test.ts` 的 horseshoe 路线只剩 2 个 outpost；扩大到
`0.54` 也仍然复现。这个问题属于路线蓝图与现有装饰安全边界的契约冲突，
不应通过复制或修改 dressing 规则掩盖。

因此采用 `roadWidth=0.50`、`shoulderWidth=0.12`：路芯约为节点直径的 40%，
完整带宽约为 60%，并通过五种路线形状的 outpost 约束。配合暖奶油色 soil
材质，它在真实页面的有效像素里仍能读成连续的路带；`roadWidth=0.68` 的
视觉收益不足以抵偿该确定性回归。

### 镜头选择

`COURSE_POLAR=68°`（离地平线 22°）与 `COURSE_DISTANCE=36` 是最终课程落地姿态。
`60°` 更像地图俯视图，`72°` 及以上把近处草和石块推成前景墙；`68°/36`
在桌面同时留下头像、天空/海的纵深带和约五个后续节点，手机上也比更近档位少裁掉
路径。旋转锁定保持不变，`COURSE_DISTANCE_MAX=76` 继续允许拉远查看全岛。

## 八、背光黑洞的明度修复（2026-08-28）

这次问题不是造型缺失，而是课程岛背光坡面被压成了接近纯黑的大片区域。
判定使用临时 Playwright 脚本：固定 1440×900、deviceScaleFactor 1，访问课程岛、
岛群图和星球页，等待 16 秒后通过 CDP `Page.captureScreenshot` 取图；只对 canvas
像素做 sRGB 相对亮度直方图，统计 `L < 0.08`。没有把色相分割的陆地比例当主指标，
因为纯黑没有色相，会被错误地从陆地遮罩中排除。

| 视图 | before：`L < 0.08` | after：`L < 0.08` | 变化 |
| --- | ---: | ---: | ---: |
| 课程岛 | 309,460 / 1,296,000 = **23.8781%** | 153,299 / 1,296,000 = **11.8286%** | -12.0495 个百分点 |
| 岛群图 | 12,299 / 1,296,000 = **0.9490%** | 4,493 / 1,296,000 = **0.3467%** | -0.6023 个百分点 |
| 星球页 | 12,295 / 1,296,000 = **0.9487%** | 4,495 / 1,296,000 = **0.3468%** | -0.6019 个百分点 |

课程岛的黑暗像素占比下降 **50.5%**；after 的 scene-linear 采样为
p05 = 0.121、p95 = 0.704，说明暗部被抬起而受光面仍在原来的明亮带内。地图和星球页
没有出现可见过曝或课程节点结构损失。

### 采用的共享光照值

| 参数 | before | after |
| --- | ---: | ---: |
| `WORLD_SUN.keyIntensity` | 9.0 | **5.2** |
| `WORLD_SUN.hemisphereIntensity` | 0.3 | **1.3** |
| `WORLD_SUN.ambientIntensity` | 0.1 | **0.4** |
| `WORLD_ENVIRONMENT.intensity` | 0.1 | **0.8** |
| `WORLD_SUN.hemisphereGround` | `0x7f8b8e` | **`0x8a5b45`** |

按用户可见的主光 /（半球光 + 环境光 + PMREM）计算，光比从 **18:1** 降到
**2.08:1**；不把 PMREM 计入的直接灯光比仍为 `5.2 / (1.3 + 0.4) = 3.06:1`，
保留了课程岛低模阴影的形体读法。暖棕色下半球反弹让剩余暗部保有颜色，而不是中性灰黑。

`Stage.tsx` 的 `toneMappingExposure` 仍为 1，ACES 与 sRGB 编码仍由现有独立 grade
pass 负责；阴影、68°/36 单位相机和 `island-grass*` 均未改。`environmentLight` 仍是
同一套程序化天空 PMREM 写入 `scene.environment` 的技术，只调了强度，因此没有改 ADR-0008
的技术锁。

中等补光实验仍留下约 17%–19% 的课程画面低于阈值；最终选择这一组 2.08:1 参数，是因为
它把黑洞减半，同时把地图、星球页和课程岛的受光面保持在可读范围内。

## 2026-08-29 · 两条被推翻的分支，和它们证伪的东西

两条分支各自跑完、各自诚实报了数、都没有合并。留在这里的是它们证伪的结论——
按本文件的规矩，被推翻的假设比成立的假设更值钱，因为它是下一个人不用再走的死路。
分支已删，SHA 记在下面以便 `git show` 追溯。

### `work/grass2`（`4268899`）：草的缺席不等于露出沙土

它把「这里没有草」和「这里把地面混成土砂色」**绑在同一个 field gate 上**
（`island-geometry.ts` 的 exposed-ground 混色，`ISLAND_EXPOSED_GROUND_MIX_MAX` 提到 `0.86`），
再叠加把 course desktop cap 从 80,000 降到 30,000。

结果是近景出现大片发亮、发黄、没有草叶层次的地面：草没有归零，
但**被切成少数高饱和绿簇，其余地面太亮太空**，整体读成一张光滑的黄绿色板。

**证伪的结论**：局部缺草可以由结构（路线 / 坡度 / 岸线）驱动——这个想法是对的，要保留；
但**缺草与地面换色必须解绑**，且换色强度需要自己的上限。
一块没长草的地，应该仍然是**地**的颜色，不是沙滩。

它同时把 `IslandFoliage` 的投影换成了通用 `AssetField`，副作用是树从有层次的自然组团
变成规整重复的低多边形柱状体。**换树的绘制路径要单独验收，不要夹带在草的改动里。**

### `work/archipelago-identity`（`c6913cb`）：色相在低彩度小岛上不成立

它给课程岛加了身份色，`surfaceStrength = 0.72 + share * 0.28`——**0.72–1.0 的近全覆盖**
（`island-surface-style.ts` 在 shared look 之后再次把 `diffuseColor` 混向身份色）。
课程视角下整块地形被染成青蓝，绿地、土坡、岩面之间的材料层次被冲掉。

三个候选沿「色相 vs 剪影」这条轴分布。实测：

- 最靠色相的候选（`hueShare=0.70`）与中间候选**在肉眼下没有区别**，六座岛仍是同一种浅麦色
- 最靠剪影的候选（`hueShare=0.22`）**明显可分**：细长、敦实、宽，形状差异读得出来
- 分支自报 CIEDE2000 最小值从 `29.26` 掉到 `16.93`

**证伪的结论**：**色相要有彩度才读得出来。** 在接近中性的浅褐色上、在 50px 的尺寸上，
≤18° 的色相旋转等于没转。这不是参数没调够，是这条杠杆对这个素材无效。
ADR-0009 早就写了世界尺度只画「剪影 + 一道明暗断裂 + 一个高光」——**里面没有色相**。

**下一次重做时轴应该是「剪影强度 × 彩度」**，色相只作为剪影已经拉开之后的补充，
并且必须先证明彩度足以承载它。低彩度本身可能是光照的后果，所以**先修光照再判身份**。

### 一条关于尺子的记录

行星页不要用「暗像素百分比」这把尺子。行星坐在黑色太空背景上，
不管光照怎么改这个数都在 79% 左右——**它量的是天空，不是行星**。
裁出球体、看明暗交界线（terminator）。

## 九、DOM 标签对比度复核（2026-08-29）

R2 的门槛是 DOM 标签实际投影到画面像素后的最低对比度 **4.5:1**。
这次问题不是文字颜色本身太暗，而是 `.label--icon` 与 `.label--unit` 原先把背景清空，
所以同一枚白字会在海、沙地、草和阴影上连续失去自己的可读地面。标签仍然是 DOM，
没有把文字搬进 WebGL，也没有动场景光照、地形、相机或任何 ratchet 基线。

固定种子 `foundations-before-zero` 的完整判官结果如下；before 是改动前的 main，after 是
给透明图标/单元标签加紧凑 token surface 后的结果：

| shot | viewport | before | after |
| --- | --- | ---: | ---: |
| course-design | 1440×900 | 1.3002 | **8.6739** |
| course-near | 1440×900 | 1.0490 | **7.8200** |
| course-far | 1440×900 | 1.2590 | **8.5492** |
| world-design | 1440×900 | 12.6155 | **12.6155** |
| course-design | 390×844 | 1.2522 | **8.5282** |
| course-near | 390×844 | 1.1724 | **10.2261** |
| course-far | 390×844 | 2.1876 | **10.5911** |
| world-design | 390×844 | 13.4513 | **13.4513** |

### 采用的标签 surface

`apps/university/src/styles.css` 只对原本透明的 `.label--icon` / `.label--unit` 使用
SwimmerUIKit 已有的 `--game-ui-overlay-glass-bg`、`--game-ui-overlay-glass-text`、
`--game-ui-radius-control` 与 `--game-ui-scrim-strong` token：图标是带少量内边距的圆形
标记，单元标题是紧凑圆角胶囊，阴影只做边缘分离，且关闭 live WebGL 上的 blur。这样
surface 随标签移动，只占文字自身的面积；地图仍然是主视觉，原有 lesson/study surface
没有改动。

### 被拒绝的方案

曾把所有透明标签临时换成 `--game-ui-overlay-glass-bg-strong` 的实心方块并加大内边距。
它确实能提高读数，但移动端截图中图标变成醒目的黑色方块、单元名变成黑条，路径被盖成
一串 UI 印章，违背地图的空间感，因此不合并。证据保留在
`SHOTS/island-look/rejected-solid-plate-mobile.png`。只加 text halo 也没有采用：它对场景
像素的最坏背景没有提供稳定的地面，不能单独满足这把实际像素尺子。

前后截图与逐项验证记录属于一次性的执行证据，不随本文件长期维护；本节记录的是结论
与它的量度，而不是那次会话的产物清单。

## 十、课程网格的形状反例（2026-08-30）

本轮视觉复核确认：当离散主区域从 route cell 开始生长时，长课程的岛会退化为路线的
胖版本。路线可以贯穿前后两端，但这不代表路线应该决定土地的轮廓；两者必须是先生成
独立地点、再把路铺进去的两个阶段。

形状尺子放在 `packages/world/src/grid/grid-outline.ts` 的纯函数
`gridRegionShapeMetrics()`，使用 exposed hex edges 计算 `perimeter² / area`，并辅以
axial hex 的 radius of gyration。当前 41 课门槛是 `<72` 与 `<9.5`。旧的路线驱动主区
实测 `perimeter² / area = 105.4756`，专门的 41 格直条反例会让测试变红；当前独立紧凑
区域通过同一门槛。

最终规则：`growMainRegion(seed, target)` 只能依赖中心、邻居数和种子噪声，不能接收 route；
`course-grid.ts` 随后将 blueprint anchors 拟合到 `mainCells`，在允许区域内寻找相邻路径。
如果路径真的放不下，重试扩大区域及其物理 footprint；不得通过拉直路径或让路径决定岛形
来伪装适配。这个反例也写入 `grid.test.ts`，避免以后只凭截图回归到路线形状。

## 十一、岛群图从海面改成浮空（2026-08-30）

这次复核把简报里的三个视觉问题放回同一张固定镜头看：连续的青绿色底板让所有课程
像停在一张海图上；旧的窄灰米色带像第二个渲染平面；远景底面太浅，课程岛没有「浮」的
垂直证据。先看图后的结论是：问题不在再画一张更复杂的海，而在于把海这一层撤掉，让
天空成为负空间，再让底面承担浮岛的剪影。

### 被推翻的结论

上一轮的结论是「岛群图需要一张受光的航拍海面」。它在课程近景有局部收益，在岛群图
却把大部分画面压成连续青绿色，并且把暖色 horizon 变成像贴图边界的窄带。这个结论被
本轮真实页面截图推翻：**岛群图不需要可见的海面；需要的是天空、云和随距离收敛的空气。**

### 采用的规则

- `WorldScene` 继续复用唯一的 `SkyDome`，但 world 投影使用浅蓝的 horizon / nadir；
  `CuteCloudSea` 继续作为现有的两批实例化云层。没有新建第二份 sky 或 cloud 实现。
- world 的 `includeSea` 由 `WORLD_SKY_CONTRACT.visibleSea = false` 控制，`DeepSea` 和
  可见的 `AerialWorldPlate` 不再提交到画面；隐藏的 aerial plate 仍保留加载信号，供
  `island-look` 判官识别资源是否 ready。
- `FogExp2` 只加在 world 的 weather 上，远端系数写在同一个 contract 中。它是空气透视
  的廉价实现，不加网格、不加 draw batch；远岛仍保留色块，但会向 lower air 收敛。
- `WorldUndersideField` 仍然是两批实例：一个共享六边倒锥底座、一个共享三角低模岩刺。
  每座远景岛按 cell footprint 生成 3–5 根岩刺，倒锥深度随 footprint 增长并封顶；纯规则
  与 triangle budget 在 `world-underside.ts`，没有每门课一份装饰表。
- 瀑布没有加入 world 远景：固定镜头下远景岛的底面只有约几十像素，透明瀑布不会形成稳定
  的可读形状，反而会增加排序与预算风险。课程岛原有的 waterfall 实现没有改动。

### 固定镜头的实测（`post=off`，1440×900）

下表的数值来自同一 capture 脚本写出的 JSON；`gl.info.render` 统计包含完整渲染帧，
不是代码里的估算值。远景底座和岩刺虽然更深，移除可见海面后总 draw / triangle 仍下降。

| seed | before calls / triangles | after calls / triangles |
| --- | ---: | ---: |
| `foundations-before-zero` | 34 / 69,022 | **32 / 68,828** |
| `product-website` | 34 / 69,090 | **32 / 68,896** |
| `generated-assets` | 34 / 68,858 | **32 / 68,664** |

移动端同一组截图也保持下降：foundations 为 34 / 58,690 → **32 / 58,496**，
product 为 34 / 58,758 → **32 / 58,564**，generated-assets 为 34 / 58,526 →
**32 / 58,332**。截图和原始 JSON 由本次 `.scratch/SKYWORLD.md` 报告列出；这份契约只
保留结论和可复核的数值，不把一次性截图目录变成长期索引。

## 十二、整个场景的体积感复核（2026-08-31）

本轮把简报的范围从「飞岛底面」还原成真正的问题：固定机位下，天空、云、岛体和水都
必须有前后关系。先在同一机位拍了 `post=off` 与 `post=on` 的基线，再改共享规则；参考
图只借它的暖色天空、粉/蓝/黄的关系、三瓣云冠和深色同色系底面，不把天空、云或水做成
贴片。云仍是三维闭合体，水仍是三维闭合体；另一个分支的兔子骑云不会撞到一张贴图。

### 被推翻的结论

- 「课程岛原有 waterfall 实现没有改动」只对本节之前的版本成立，现已被推翻：瀑布原来
  是带色带的 `PlaneGeometry`，轮廓有颜色却没有侧面和背面；现在它是同一批次、同一预算
  的浅盒体，并保留下缘外扩。
- 「云和水的平感主要靠继续调灯」也被推翻。`post=off` 已经证明平感来自课程云的
  `ShapeGeometry` 和瀑布平面；它们没有可被光照解释的法向体积。`post=on` 再把共享褐色
  底面、AO 上限和暖色 grade 一起放大成黑红块，所以根因是**几何/值域管线与后处理的
  组合**，不是单独把太阳再调亮。

### 采用的规则

- `cloud-volume.ts` 是云的唯一体积几何帮助器。课程云用三瓣闭合低模 bank，岛群云继续
  用原有的球形 lobe 语言；两者都只在 `useMemo` 建一次，值域渐变烘在顶点色里。
- 课程云保留三层、7 个实例和原有的三批结构；水保留一个 mesh。透明闭合体使用
  `FrontSide`，避免 `DoubleSide` 的隐性双 draw；`MeshBasicMaterial + vertexColors`
  不引入新的环境纹理采样。
- `gridUndersideColorForTop()` 是跨课程/跨投影的纯函数：底面 78% 继承当前岛面色相、
  22% 回到共享土色，远景 `WorldUndersideField` 仍然只有两批实例。旧的褐色常量只保留
  为 inspector 的回退色，不能再无差别涂所有岛。
- AO 从整面压暗改为接触折痕；grade 降低对比度和暖高光、把 pivot 放到低暗部。这样
  `post=on` 仍有戏剧性，但不会把草土的绿/蓝通道裁成红黑。

### 固定机位的实测（`post=off`，1440×900）

`gl.info.render` 统计来自完整真实浏览器帧，不是代码估算。改动没有新增 pass、draw
  batch、纹理采样或每帧 CPU 工作；课程云单体为 37 → **36 triangles**，瀑布为
  96 → **96 triangles**，总场景预算如下：

| 镜头 | 改前 calls / triangles | 改后 calls / triangles |
| --- | ---: | ---: |
| course-design | 26 / 19,913 | **25 / 19,810** |
| course-near | 25 / 19,905 | **24 / 19,802** |
| world-design | 33 / 62,261 | **33 / 62,261** |

可见 Chrome、1440×900、`freeze=0` 下各测 3 轮、每轮 180 个 `requestAnimationFrame` 间隔：
改后 `post=off` 平均 16.671 ms、p95 最大 18.5 ms；`post=on` 平均 16.665 ms、p95 最大
18.4 ms。`document.visibilityState` 为 `visible`；这里报告的是显示帧间隔，不把隐藏页的
节流时间冒充渲染耗时。

课程矩阵按 `foundations-before-zero`、`foundations-terrain`、`identity-and-accounts` 三个
seed，再乘 6/12/24/41 节、arc/horseshoe/loop-around-hill/switchback/serpentine 五种
形状和三个机位，共 **180** 个固定截图复核。长短课程的云冠、瀑布和底面没有退化成绿色
平板；岛群图继续没有可见海面，远景底面继续保持两批实例。这个结论由
`packages/world/src/sky/cloud-sea.test.ts`、`packages/world/src/grid/world-underside.test.ts`
及底面色相测试守住，而不是只靠一次截图。

## 十三、课程岛同族绿的坡度受光复核（2026-09-01）

第五轮把课程岛的色相收回到连续的绿色家族，却同时删掉了大部分明度铺开。第六轮先看
`post=off` 的固定压力样本，再看 3 个 seed × 4 个课量 × 5 种路线 × 3 个机位的 180 张
矩阵。结论不是把地表重新分成颜色带，而是让同一块绿地拥有真实的受光肩和背光肩。

### 被推翻的结论

- 「把课程四级地形值阶拉开，就能恢复明度」被推翻。它会把同一片草地切成柠檬黄、橄榄和
  褐色的硬块；`GRID_TERRAIN_VALUE_RAMP.course` 现在固定为 `[1, 1, 1, 1]`，不再承担
  课程地表的明暗。
- 「把共享 key 直接推到诊断用的 9.0 就能过尺子」也被推翻。它确实抬高了 P95/P98，
  但固定机位出现发亮的黄绿色面，且会同时改变 world；没有合并。
- 初版只移动顶面、底面不跟随的实现被推翻。它会把每块棱柱的上沿拉斜，放大相邻单元间的
  土壁条带。现在课程 land 的整个棱柱跟随同一个局部坡度倾斜，顶面法线也使用同一坡度。

### 采用的规则

- `gridSurfaceSlopeFor()` 从相邻 cell 的真实 `topY` 梯度开始，再叠加沿共享太阳方向的
  低频连续 relief、横向微小 relief 和边缘肩；它只产出几何坡度，不产出颜色。
- `HexField` 把坡度放进每个课程 land instance 的 `gridSlope` attribute，倾斜棱柱并重算
  顶面法线。route、bed、detached、world 都拿零坡度；因此课程视觉仍是一个 renderer，
  但不会把作者地图的其它投影偷偷改亮。
- 在本节复核时，课程 land 的 albedo 仍来自原有 course palette，`GRID_TERRAIN_VALUE_RAMP`
  还是 `[1, 1, 1, 1]`；这个「完全不做地形值阶」的局部结论已被 §16 的真实截图推翻，现行
  规则改为窄 ramp 和连续低频色场。§16 没有新增按 cell 编写的黄/褐色表，也没有用小白块
  推 P95/P98；lesson accent、树和泥土身份色仍是既定产品语义。

### 固定压力样本的历史实测（`post=off`，1440×900 / 390×844）

下表记录的是本节 2026-09-01、窄 ramp 之前的版本；当前地表修订及其前后值见 §16 和
本轮执行报告。

下表只列本轮五条明度带和两条色相带；每个值都来自真实浏览器画布。色相两列在 8 个机位
全部通过，明度红项保留在这里供下一轮决定，不改硬带、不改 ratchet。

| 机位 | grass spread ≥45 | land rise ≥15 | land P95 ≥85 | lightness P98 ≥90 | stddev ≥18 | hue count / spread |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| course-design / desktop | 26.3171 🔴 | 19.2908 ✅ | 80.1963 🔴 | 84.0965 🔴 | 16.3650 🔴 | 7 / 119.4828 ✅ |
| course-near / desktop | 42.7705 🔴 | 17.2206 ✅ | 78.9399 🔴 | 83.4506 🔴 | 15.8306 🔴 | 7 / 95.7000 ✅ |
| course-far / desktop | 38.7361 🔴 | 17.5707 ✅ | 80.8968 🔴 | 83.3423 🔴 | 17.5051 🔴 | 8 / 119.7458 ✅ |
| world-design / desktop | 57.1266 ✅ | 39.1637 ✅ | 94.8376 ✅ | 97.7895 ✅ | 22.0416 ✅ | 9 / 120.0000 ✅ |
| course-design / mobile | 19.2870 🔴 | 26.1524 ✅ | 77.4898 🔴 | 83.8095 🔴 | 12.6685 🔴 | 6 / 119.1368 ✅ |
| course-near / mobile | 9.9159 🔴 | 16.7274 ✅ | 78.4466 🔴 | 84.4141 🔴 | 13.2554 🔴 | 5 / 78.7500 ✅ |
| course-far / mobile | 36.6701 🔴 | 18.2700 ✅ | 80.5593 🔴 | 82.3534 🔴 | 17.5174 🔴 | 7 / 93.7500 ✅ |
| world-design / mobile | 56.0917 ✅ | 39.6531 ✅ | 95.1943 ✅ | 98.7695 ✅ | 26.9893 ✅ | 9 / 120.0000 ✅ |

课程 land 的 median 仍在 50–70，P2 也没有越过 ≤25 的暗部上限；未满足的 P95/P98/stddev
和部分 grass spread，来自固定近景只看见同一坡面、以及继续保留暖 key/fill 防止土壁成为
黑洞。把 key 推到 9.0 或重新加入宽 albedo ramp 能让数字过线，却会让画面出现死亮黄块或
褐色拼贴，违反本节真正的视觉目标，因此不为这些数字牺牲图像。

官方 `pnpm e2e:island-look` 仍按要求使用原 ratchet，第一机位在
`course-design/desktop/landLightnessRise` 以 `19.2908 < pinned 24.7634` 停止；没有
修改判定、阈值或 pin。独立矩阵复核 180/180 完成、0 个页面错误；矩阵中 course-near
的分位数会随镜头裁切而变化，这也是不把矩阵最差值伪装成固定压力样本结论的原因。

## 十四、低太阳仰角试验（2026-09-01）

本轮只动共享 `WORLD_SUN.elevationDeg`，没有改材质、曝光、硬带或棘轮。先在当前
六边形课程岛的 24° 基线上跑 `pnpm e2e:island-look`，确认截图不是黑屏；再把仰角降到
18°，最后试到已测安全窗口的下界 16°。以下都是同一压力课程、`course-design`、桌面
1440×900、`post=off` 的真实浏览器画布读数。硬带仍是合同中的固定地板。

| 硬带 | 24° 基线 | 18° 试验 | 16° 试验 | 边界 |
| --- | ---: | ---: | ---: | ---: |
| 陆地 p95 明度 | 80.1963 | 76.8367 | 75.6951 | ≥ 85 |
| 背景明度跨度 | 22.4622 | 22.4622 | 22.4622 | ≥ 40 |
| 草地明度跨度 | 26.3162 | 25.1133 | 25.8156 | ≥ 45 |
| 草地 p95 明度 | 81.0970 | 78.3863 | 77.4716 | ≥ 85 |
| 全画 p98 明度 | 84.0965 | 84.0164 | 83.9071 | ≥ 90 |
| 全画标准差 | 16.3643 | 16.0951 | 16.0563 | ≥ 18 |

18° 和 16° 都没有产生可见的高光带；它们让课程岛的草地与受光面继续变暗，背景跨度
甚至完全不动。16° 已经到安全窗口下界，继续降低会进入 `sun.ts` 已记录的
`measureScene().p05` 不可用区间，而且当前截图趋势是在损失亮部，不是在恢复形体。因此
本轮在 16° 停止，并把运行时恢复为 24°。没有重钉历史 ratchet，也没有为了过数改合同。

## 十五、判官一直只检了第一个机位（2026-09-02）

`assertIslandLookRatchet` 把 `expect` 写在逐项循环里，而这个循环的调用点又在机位循环
里面。于是第一条越界就结束整个 run。第一条越界从 2026-08-30 起一直是
`course-design/desktop/landLightnessRise`（19.2908 < pinned 24.7634，第十三节记着同一
个数），**它后面的 19 项和另外 7 个机位从来没有被比较过**，而 `metrics.json` 写在机位
循环之后，所以那个文件在红的时候根本不存在。

改法只动报告，不动 pin、不动硬带、不动场景：越界改成跨机位累积，`metrics.json` 先落盘，
最后一次性失败并列出全部。改完在安静机器上重跑一次，结果：

| | 数量 |
| --- | ---: |
| ratchet 越界（对 2026-08-30 的 pin） | **42** |
| 硬带越界（对 `ISLAND_LOOK_CONTRACT`） | **39** |
| 之前这条命令能看见的 | **1** |

两条必须分开读：

- **pin 是 2026-08-30 的，场景在 08-31/09-01 被有意重建过**（云成体、瀑布闭合、底面按
  色相取暗、地表明暗改由高度和坡度产生）。所以 42 条里大部分是**已被接受的改动的后果**，
  不是回归。第十三节那张八机位表已经量过其中大部分。
- **真正之前没人看见的是 `course-design/mobile/accentArea = 0.0079`**，硬带是
  `{min: 0.015, max: 0.15}`——它低于合同自己的下界，而且不在第十三节那张表的六列里。

**不要为了让这条命令变绿去重钉 pin。** pin 陈旧是事实，但重钉是一次需要人眼看过实物
才能做的决定：42 条里哪些是「接受的代价」、哪些是「悄悄变差」，只有看图能分。在那次
人眼复核发生之前，这条命令保持红，并且现在它至少把全部真相一次报完。

**运行时间**：全量八机位约 25 分钟。之前那个 10 秒的 run 不是快，是在第一个机位就停了。

## 十六、课程地形表面倒角复核（2026-09-02）

本轮只处理六棱柱的几何/法线、三档接缝、地表固有色、崖面层理和岛缘压边；灯光、天空、
调色与 AO 均未改。先看固定压力课 `foundations-before-zero` 的真实页面，再保留能让
课程岛读成游戏地形、又不把同高草地切成砖盘的规则。

### 两条倒角路径的实测结论

实测了两条路径，第三条「只改法线分组」没有作为独立路径采用：

- **全场实体倒角**：每个棱柱由 18 个三角形变成 30 个，`course-design` 为 45 / 35,574
  → 45 / 43,470，`course-near` 为 26 / 20,506 → 26 / 28,402；draw call 不变。
  路线节点确实更像砖，但同高 land 也获得重复倒角，近景出现暗三角缝和板边感，违反
  §13 的「同高度接近无缝」，否掉。
- **着色器伪倒角**：不增加几何，设计机位仍为 45 / 35,574、近景仍为 26 / 20,506。
  修正编译错误后再看有效截图；它只留下很轻的边缘明暗提示，路线节点仍像平面色块，
  没有达到参考图中倒角吃光的效果，否掉。

最终采用**按功能选择的实体倒角**：route 与 detached 使用 30 三角形并执行
`computeVertexNormals()`，land 与 bed 保持 18 三角形；固定压力课最终为
`course-design` 45 / **36,090**、`course-near` 26 / **21,022**。相对本轮基线，
两机位均为 **+516 triangles、+0 draw call**；额外几何只落在 43 个路线/孤立块实例上。
这是 §13 三档接缝的实现：land `-0.02`（轻微重叠），route `0.072`，detached `0.04`。

### 表面规则

- 外缘压边石使用共享低饱和暖石色 `0xa8875c`，只给主区域轮廓和 detached cell 写
  `gridRim` 实例权重；没有把每个六边形都画成金线，因此它读成建造过的岛缘而不是廉价
  描边。
- 崖面仍沿共享 cliff/shadow 材质响应，但加了一条按深度重复的窄横向岩层带，并把课程
  侧面固有色保底从 `0.58` 提到 `0.78`。这是材质值域变化，不是灯光提亮；最终图里前缘
  能看到暖褐层次，阴影侧没有压成纯黑。
- 课程地形值阶从 `[1, 1, 1, 1]` 收窄改为 `[0.92, 0.97, 1.03, 1.08]`；unit
  territory 的值差由 `0.94..1.06` 放到 `0.92..1.08`，逐格 hash 差由 `0.06` 收到
  `0.03`，另加连续低频世界坐标色场。图上是大块连续起伏，不是逐格棋盘。

固定截图、逐轮否决证据和完整指标 JSON 留在对应 worktree 的 `.scratch/surface/shots/`
与 `REPORT.md`；本节只保留可以约束后续实现的结论。
