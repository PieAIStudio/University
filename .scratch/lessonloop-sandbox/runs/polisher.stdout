# 屏幕上的按钮，代码里能找到对应的哪几行？


## 先看屏幕上的界面

先看看《图灵密约》跑起来是什么样。这是它每天出一道题的页面——聊天记录、你选的人、最后的答案，全在同一屏里：

:::figure[真实界面：每天一题的结果页]{#turing-pact-daily-result}

真实截图 · 英文界面 · 每天一题那一页 · 已选中一名候选人，结果面板已展开。
:::

:::figure[标注：三块区域各在哪]{#turing-pact-daily-result-annotated-crop}

同一张截图加上标注：对话区、结果状态和候选人按钮分别落在哪。
:::

:::figure[放大：一个候选人按钮对应的界面元素]{#turing-pact-daily-button-focus}

只放大一个候选人按钮。它在屏幕上是一个能点的界面元素。
:::

你点其中一个候选人按钮，同一屏就会显示对错。画面会变，但不是换成另一张贴上去的图。

:::detail[这些对话和按钮，真的是一张图片吗？]
不是。这里的对话文字、候选人按钮和结果，都是程序照着代码生成、摆在页面上的。开发者把代码写在文件里，浏览器读了文件，才把它们画成你看到的界面。你点按钮时，程序收到点击动作，更新选中的人和结果，所以内容直接在当前页变，而不是换成一张静态图。

**打个比方：** 代码文件就像一份“施工说明”，屏幕是电脑照着说明搭出来的样子。这个比方只帮你理解“文件和界面有关系”；实际上依然是程序读代码、画界面、响应点击。
:::

## 接下来，我们从一个更小的例子开始

刚才那是屏幕上画好的样子。平时你点图标进游戏；但在开发者眼里，看到的只是一堆能编辑的文本文件。咱们先从最外层的网页入口文件 `index.html` 看起。它开头两行长这样：

[[evidence:index.html:1-2]]

:::detail[`index.html` 在页面加载时负责什么？]
`index.html` 是网页打开时首先被读取的入口文件。它用标签搭出基本骨架，并告诉浏览器接下来去读哪个脚本；它本身只是一段文本，并不是显示出来的完整页面。

**打个比方：** 把它看成入口处的指引牌：告诉你从哪进、下一步往哪走，但不会把整栋楼的每个房间都画在牌子上。
:::

再往下看，第 18 行写着一个很短的名字：

[[evidence:index.html:18]]

这几个字会出现在浏览器窗口最顶上的那一小条标签里。

:::detail[第 18 行那个名字会出现在哪？]
就出现在浏览器标签页那一小条上——你同时开很多网页时，就是靠这几个字认出「这是哪一页」。改掉这行字、保存、重新打开页面，那一小条上的字就跟着变了。
:::

最让人意外的是文件末尾。留给「页面身体」的地方几乎是空的——只留了一个空位，加了一行「接下来去读哪个文件」的指示：

[[evidence:index.html:29-31]]

屏幕上明明是一整页对话和按钮。可到了入口文件末尾，留给页面身体的地方却几乎是空的。

## 先猜一下

只改入口文件第 18 行的名字，保存后再打开页面，屏幕上的候选人按钮会不会跟着变？

先写下你的判断，再往下看答案。

## 答案

不会。候选人按钮不写在这份入口文件里；改第 18 行，变的不是那些按钮。

## 为什么屏幕上的东西能和文件里的代码对应？

网页是用一种带记号的文本写成的。

:::detail[什么叫「记号」？]
这里说的记号，就是夹在尖括号里的词，比如 `<title>`。它不是给人读的内容，而是写给浏览器的说明：哪里是标题、哪里是一块区域、哪里到此为止。浏览器靠这些记号认出一页的骨架，再决定字号大小、哪一块摆在哪。写代码的人管它叫「标签」——跟商品价签、社交平台的话题标签没关系，只是名字撞了。
:::

回头看刚才见过的三处：第 1 行告诉浏览器「这份文件按网页规矩读」；第 18 行管的是窗口顶上那条窄标签里的字；末尾几行留好空位，点名让浏览器去读 `main.tsx`。

入口文件几乎是空的，因为它只负责占空位，并点名接下来该读谁。

:::detail[为什么入口文件几乎是空的？]
因为真正的按钮、对话和排版，都不写在入口文件里，而写在后面读取的文件里。入口先占个空位、交代一句「下一份读谁」；等那份文件跑起来，再把界面填进空位里。所以你看着入口会觉得「怎么这么空」——空，是故意留的。
:::

浏览器顺着「去读 main.tsx」这行指示走下去，才会开始画你看到的界面。

:::detail[`main.tsx` 那种文件又是什么？]
它是另一类代码文件，里面写的是「先做什么、再做什么」的步骤。浏览器读完入口文件、看到指示，就会照着那份文件里的步骤做——你看到的按钮和排版，多半是这样一步一步画出来的，而不是在入口文件里事先写死的。下一节课咱们顺着这条线往回追。
:::

每天一题这一页，代码写在 `src/pages/DailyPuzzlePage.tsx` 里。入口点名 `main.tsx` 后，程序才接着把这一页画出来。下面这段代码第一次看会觉得很长，先不用慌，只盯外壳：顶栏、题目、任务说明。

:::detail[这些吓人的代码，怎么才能不吓人？]
代码看起来可能很长、很陌生，但你不用一次把它们全背下来。AI 可以帮你写代码、改代码；你学这些基础，是为了把需求说清楚，听懂 AI 在解释什么，并检查它改得对不对。跟着课程从一个按钮、几行代码看起，你会慢慢建立这种判断力。

**打个比方：** 你不需要先学会造汽车才能看懂仪表盘；但你得知道仪表盘亮灯代表什么，才能跟修车师傅沟通、发现不对劲。
:::

[[evidence:src/pages/DailyPuzzlePage.tsx:300-379]]

回头看刚才放大的候选人按钮。代码照着名单逐个生成按钮，并把点击登记上去。你点其中一个，当前页就会记下你选的人。

[[evidence:src/pages/DailyPuzzlePage.tsx:396-499]]

出了结果之后，同一页会接着画出对错和分数。它不是换了一张图，而是基于同一套页面数据继续画：

[[evidence:src/pages/DailyPuzzlePage.tsx:544-581]]

所以，屏幕上的按钮能在 `src/pages/DailyPuzzlePage.tsx` 里找到对应的行；入口文件第 18 行管的则是浏览器顶部的标签。手机上的图标是交到你手里的入口，`index.html` 是网页加载时用到的入口。

## 再想想

那个几乎什么都没写的空位，下一课咱们沿着 `main.tsx` 往回追：[[lesson:empty-box-called-root|空盒子是怎么被填满的]]。

## 自检

**问：** 你手机上点开的那个图标，是不是就等于 `index.html` 这个文件本身？

## 一句话

**在开发者眼里，App 首先是一堆能打开、能修改的文本文件，而不是一个打不开的黑盒子。**
